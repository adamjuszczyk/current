# CONTEXT.md — Current

## What this is
Current — a personal project/direction-tracking app. V1 shipped and is in real daily use; v2 is specced and planned against what that use showed. This file is the single source of truth for current app state; keep it lean and update it after every session, per usual convention.

## Stack
React, TypeScript, Vite, Tailwind, Supabase, TanStack Query, Zustand, Dexie, Recharts, vite-plugin-pwa.

## Database
Fresh, dedicated Supabase project. No shared backend or cross-app data access with Overload.

## Current build target
**V2 sandbox.** Spec: `CURRENT-V2-SANDBOX-SPEC.md`. Plan: `TASKS.md`. Still a sandbox, still no visual design pass — but no longer a guess: every feature in it traces to a specific instance of using v1 for real, which is exactly what v1 was built to produce.

Scope: what a Block contains is replaced by a project canvas inside a resizable popup, holding Lists (step-by-step or flat) and Notes; Connect adds block-to-block connections with graph auto-layout; Waiting adds project-level blocking built from entries, with a derived Ready state; Focused marks what's being worked on and gets its own screen; a three-way Filter controls what the canvas shows; and Visuals carry the states that aren't otherwise legible. Plus the one v1 fix: text inputs that expand with their content.

Carried forward from v1 unchanged: Areas, single-account auth with RLS, confirmation on every delete, Area-level Free Notes, one kind of Block, and the three manual statuses.

**Not in scope for this build:** Vision, Phase and everything on it, theming, any visual design pass, task-level Waiting, shared tasks between blocks, "Today", an explicit next-step pointer, projects-within-projects, zoom/pan, and fullscreen/tab mode. A fuller v2+/vision-scale version of this product exists, but is kept outside this repo on purpose, so it can't shape this build. It is not present here — don't look for it, don't reconstruct it, and its absence is not a gap to flag or escalate. Everything needed for this build is already in `CURRENT-V2-SANDBOX-SPEC.md`.

`CURRENT-V1-SANDBOX-SPEC.md` and `TASKS-V1.md` are kept as the record of what v1 was built to. The v2 spec supersedes the v1 spec entirely; neither v1 file is the plan of record any more.

## Status
**V1 is complete, verified and live — closed 2026-09-09.** All six of its phases were built, all 26 items in `TASKS-V1.md` are done, and both acceptance criteria were met against the live Supabase project: 0.3's RLS check by `npm run verify:rls`, and 2.4's cascade delete by Adam in the running app. Login, Areas, Canvas, Blocks, Tasks and Free Notes all work end to end, and have been in real daily use since. That use is what produced the v2 spec.

**V2 Phase 1 is built and verified — 2026-09-10.** `TASKS.md` holds eight chunks — Phase 1, Phase 2a, Phase 2b, and Phases 3–7 — and 43 items, generated from `CURRENT-V2-SANDBOX-SPEC.md`. Everything the running app does is still v1 unchanged, except how Note and task text fields render (Phase 1) — v2's schema and client changes (Phase 2a) are written and proven against a scratch database but **not** run against the live project yet, so the live app is still v1's shape.

**Phase 2a (schema, RLS, client compatibility) is built and proven against a scratch database — 2026-09-11.** `0002_v2.sql` is written, run fresh against a scratch Postgres 16 seeded with representative v1 data, and every task survives under its original block in a new Flat list with completed state intact; a block with no tasks gets no list. RLS on the four new tables is verified by direct SQL against the same scratch database (no PostgREST/GoTrue in this environment — see Phase 2a's build-log entry for what that means and doesn't mean). The client (`useTasks` keyed by list, notes hooks handling either parent, `BlockEditPopup` reading its block's list, quick-capture routing through one Flat list) is verified end to end in a browser against that same scratch database via a hand-rolled PostgREST stand-in, not a hand-written fixture. **None of this has touched the live project** — that is Phase 2b, gated on a confirmed backup, still not started.

**The database is no longer empty, and that changes the shape of the risk.** V1's migration had nothing to lose; v2's Phase 2b rewrites live rows — tasks move out of blocks and into lists. Phase 2 is split on exactly that line: 2a writes and proves the migration against a scratch database (now done), 2b is the one irreversible step, applying it to the live project, and it does not start without a confirmed backup.

**Phase 2 is complete.** `0002_v2.sql` and `0003_v1_backup_snapshot.sql` are both applied to the live project — via the Supabase GitHub integration's auto-apply on push to `main`, discovered the hard way when 2a's own merge applied `0002` before the backup gate it was supposed to pass through (see the build log's 2026-09-11 entries). `npm run verify:rls` passes live on all eight tables; the `v1_backup` snapshot schema is confirmed inaccessible to the anon key from outside. **The one durable process change from that incident: no session may ever create a file in `supabase/migrations/` again** — a migration file reaching `main` deploys to production by itself, so any schema change stops and asks rather than being written. Phase 3 needs no schema (`0002_v2.sql` already created everything it uses), so this bound nothing in practice this phase.

**Phase 3 (project canvas) is built and verified — 2026-09-11.** The block popup is a resizable window (native CSS `resize`, session-only size) holding a freeform canvas one level down from the Area canvas: any number of Lists (Step-by-step or Flat, kind fixed at creation) and Notes, each independently draggable, reusing `BlockCard`/`NoteCard`'s own pointer-event drag rather than a new mechanism or a library. Full task CRUD lives inside each List; Step-by-step lists gate completion and un-completion from both ends so completed tasks stay a contiguous prefix, Flat lists gate nothing. Deleting a task checks every Waiting entry on every project for a pick on it and discloses the waiting project in the confirm dialog before removing the pick and, if it was the entry's last one, the entry itself — inert until Phase 4 populates `waiting_entries`, but built and verified against seeded rows now. 2a.1's "`lists[0]` is the block's list, lazily created" stepping stone is gone entirely, replaced by real multi-list handling, which is also what closes the known create-path race `BlockEditPopup.handleAddTask` used to have — there is no longer a code path that lazily creates a list on the interactive add-task flow to race in the first place. Verified against a scratch Postgres 16 (`0001`+`0002`+`0003` applied) through a hand-rolled relay enforcing real RLS (`SET LOCAL ROLE authenticated` + `request.jwt.claims`, not a superuser bypass), driving the real `vite dev` app in headless Chromium: 33 checks passing, including the exact four-way step-list refusal sequence the phase's acceptance bar calls for (complete 2 before 1 refused; complete 1 then 2 succeeds; un-complete 1 while 2 complete refused; un-complete 2 then 1 succeeds) alongside the same operations on a Flat list in the same project, all unblocked. This container had no `.env` at session start (unlike some earlier phases' containers) — a local-only `.env` pointing `VITE_SUPABASE_URL` at the real project host (so the `sb-ieszecmiijxkcrxirwuk-auth-token` storage key matched) with a placeholder anon key was written for the `vite dev` verification run, then deleted before finishing; it was never committed. The `npm run build` this phase's diff was checked against ran with no `.env` present (201 kB, tree-shaken — see the Phase 0/1 build-log entries for what that number does and doesn't prove); `npm run lint` passes clean either way.

**Phase 5 (Focused) is built and verified — 2026-09-11.** `blocks.focused`, already present from `0002_v2.sql`, gets a client toggle on Active projects; it is mutually exclusive with Waiting in both directions (turning either on while the other is on is refused, naming which one has to be cleared first — nothing is swapped or discarded on the user's behalf), marking a Focused project Done clears Focused in the same write, a Focused block renders a real CSS outline, and a new Focus screen lists every currently-Focused project across every Area, grouped by Area, with a click that switches Area, opens the project's popup, and genuinely scrolls it into view. 29 checks passed in headless Chromium against a scratch database with real RLS enforced, including both directions of the mutual-exclusion refusal against actual database state and a cross-Area jump proven by measuring the canvas's scroll position, not just asserting the right text appeared. No schema change.

**Phase 6 (Connect) is built and verified — 2026-09-11, the last feature phase.** Blocks connect to other blocks in the same Area via a click-to-connect toggle on the canvas; acyclicity is enforced entirely client-side (a directed BFS refuses a connection whose target already reaches its source, checked *before* any mutation runs, so a refused attempt never writes a row) since Postgres cannot express reachability in a check constraint. Connected blocks are auto-laid-out by longest-path layering — a block's column is the longest path to it from any source in its component, at a uniform pitch, deterministic via `created_at` ordering within a column — and dragging any connected block moves its whole component rigidly, by the identical delta, in one batched write. Adding a connection that merges two separate components never moves the larger side's blocks (ties go to the upstream/source side); the smaller side is rigidly translated into place next to the new edge, preserving its own existing shape rather than being recomputed from scratch. Removing a connection repositions nothing, ever — a block left with zero connections drags independently again, which falls out of the same component-membership check 6.6 already needed rather than a separate flag. No schema change — `connections` already existed from `0002_v2.sql`. Verified end to end in headless Chromium against a scratch database with real RLS enforced: 30/30 checks passed, including the diamond-shaped graph the phase brief names specifically (confirming the "longest path, not shortest path" column-3 placement), a drag whose four resulting deltas were asserted numerically equal rather than merely "all moved", and a merge whose larger side's coordinates were confirmed byte-identical before and after. One genuine spec gap — what a new edge does when both its endpoints are already in the same connected component, a case 6.7 doesn't name — was resolved conservatively and documented in `TASKS.md`'s 6.5 rather than guessed at silently; see that entry and `src/lib/layout.ts`'s own comment for the reasoning.

**Phase 7 (Filter and the content marker) is built and verified — 2026-09-11. This closes the v2 sandbox build.** Three independent on/off toggles (Active, Upcoming, Done) live in the Zustand store, all on by default, transient and not per-Area; the Area canvas filters which blocks render by status and, by passing that filtered list into the already-existing `ConnectionLines` (which looks up each connection's endpoints and silently skips a line whose endpoint isn't present), a connection touching a hidden block is hidden for free rather than through a second filter. Nothing about filtering writes a block's `x`/`y` or triggers any layout pass — verified the hard way, by reading every remaining block's position back from the database, byte-identical, before and after hiding one. A block holding at least one List or Note (an empty list counts; a Waiting entry alone does not) gets a small marker distinct from Upcoming's dot, Ready's badge, and Focused's outline, so several can legitimately show at once. No schema change — Phase 7 needed none. Verified end to end in headless Chromium against a scratch Postgres 16 with real RLS enforced: 22/22 checks passed, including a drag of a visible connected block moving its filtered-out (hidden) partner by the identical delta — proving the filter never touches the underlying connection/component data BlockCard itself reads, not merely that it leaves things alone when nothing's connected — and the Focus screen still listing a Focused project whose status was filtered off the canvas, confirming Phase 5's explicit non-coupling held.

**No decisions are open.** All three from the planning session are resolved and built into the phases they govern rather than living as defaults: what happens when a project tries to leave Active while genuinely Waiting is Phase 4's 4.5; what happens when a task something is waiting on gets deleted is Phase 3's 3.5; and whether the Waiting task-picker reaches across Areas is Phase 4's 4.3 — **it does not**, so Waiting now matches Connect's explicit same-Area restriction. No phase is blocked waiting on an answer, and per the escalation criteria a *new* open decision is not to be settled with a default — the build stops and asks.

Dexie, vite-plugin-pwa, and Recharts are in the stack above but are not installed — nothing in either spec needs offline storage, installability, or charts.

## V1 — what was built

This is the app as it currently stands. Every phase number in this section is v1's, from `TASKS-V1.md`; v2's plan restarts its own numbering at 1, so "Phase 3" means different things in the two files — this heading is what tells them apart.

### Phase 0 — what was built

- Vite + React 19 + TypeScript project scaffolded at repo root (`npm create vite@latest`'s current `react-ts` template — oxlint for linting, TS project references). `npm run dev`, `npm run build`, `npm run lint` all work.
- Tailwind v4 wired via `@tailwindcss/vite` — no `tailwind.config.js`, no theme customisation, just `@import "tailwindcss";` in `src/index.css`. Defaults only, per spec.
- Supabase client at `src/lib/supabase.ts`, reading `VITE_SUPABASE_URL`/`VITE_SUPABASE_ANON_KEY` from `.env` (throws loudly if unset). `.env` is gitignored; `.env.example` is committed with empty values.
- Schema + RLS migration at `supabase/migrations/0001_init.sql` — the four tables from the data model above, plus RLS enabled on all of them with one `for all using (auth.uid() = '<owner UUID>') with check (auth.uid() = '<owner UUID>')` policy per table, pinned to a sentinel UUID that must be replaced with the real account's UUID before running (loud comment at the top of the file explains this; the sentinel matches no real user, so an unreplaced file fails closed). No `user_id` column anywhere — the policy compares `auth.uid()` directly instead. Pinning to a specific UID matters because Supabase allows email signups by default and the anon key ships publicly in the client bundle: a policy that only checked "is someone signed in" would let any stranger who signs themselves up read and write every row. The other half of the fix — disabling email signups in the dashboard so no second account can ever exist — has to happen outside this repo; see "Manual step still needed" below.
- `src/App.tsx` gates on `supabase.auth.getSession()`/`onAuthStateChange`: no session renders `src/auth/LoginScreen.tsx` (email + password via `signInWithPassword`, no sign-up form — the one account is created out-of-band in Supabase, not through this UI); a session renders a bare "Signed in as … / Sign out" placeholder, since the actual Area/Canvas UI starts in later phases.
- TanStack Query's `QueryClientProvider` wraps the app in `src/main.tsx` (`src/lib/queryClient.ts`).
- Zustand store at `src/store/uiStore.ts` holds only `activeAreaId` and which popup is open (`activePopup`/`popupContext`) — no server or persisted data, per spec.
- `scripts/verify-rls.mjs` (`npm run verify:rls`) exercises the Phase 0.3 acceptance criterion directly against the live project once `.env` is filled in: reads and writes all four tables with the anon key and no session, and fails loudly if any row is returned or any write succeeds.
- Verified locally: `npm run build` (tsc -b + vite build) and `npm run lint` (oxlint) both pass clean. Rendered the login screen in a headless browser against placeholder Supabase env vars to confirm the auth-gated shell and Tailwind styling actually render — no console errors, no crash.

### Provisioning

1. ~~Create the Supabase project.~~ Done — ref `ieszecmiijxkcrxirwuk`.
2. ~~Disable email signups in Auth settings.~~ Done, and confirmed in the dashboard directly as well as by `verify:rls`.
3. ~~Create the single account.~~ Done.
4. ~~Substitute the real owner UUID into `supabase/migrations/0001_init.sql`.~~ Done — all eight policy occurrences (four policies, each with a `using` and a `with check`), not the four a quick read suggests.
5. ~~Run the migration against the project.~~ Done by Adam via the SQL editor. No session here has, or should have, DDL access: the anon key cannot run DDL by design, and the alternatives (the postgres connection string, or a Supabase CLI personal access token) are both far broader than a one-time task warrants.
6. ~~Fill in `.env`.~~ Done in this container. `.env` is gitignored and the container is ephemeral, so anyone running the app elsewhere needs their own copy — URL and anon key are in the Supabase dashboard.
7. ~~Run `npm run verify:rls`.~~ Passed. In this container it needs `NODE_USE_ENV_PROXY=1`, because Node 22 does not read `HTTPS_PROXY` for `fetch` and otherwise egresses past the proxy the allowlist applies to.

### Phase 1 — what was built

- `src/components/Popup.tsx` — the one modal shell every popup in the spec reuses: takes `onClose` and `children`, renders via a `createPortal` to `document.body` over a click-to-close backdrop, closes on Escape, and moves focus into the dialog on open. No `open` prop — callers mount it conditionally.
- `src/components/confirmContext.ts` + `src/components/ConfirmDialogProvider.tsx` — the confirm dialog, built on `Popup`. `ConfirmDialogProvider` is mounted once at the app root (`src/main.tsx`, inside `QueryClientProvider`) and exposes `useConfirm()`, a hook returning `(message: string) => Promise<boolean>` that any component can call and await — the single gate every future delete path (Area, Block, Task, Note) must go through. Only one confirmation can be pending at a time, which is all the spec needs.
- Verified in a headless browser (temporary test buttons wired into `App.tsx` for the session, reverted afterward — not part of the committed code): popup opens and grabs focus, closes on Escape, closes on backdrop click, stays open on clicks inside its content; confirm dialog's Cancel/Confirm/Escape all resolve the awaited promise correctly.
- No new dependencies. No delete paths, Areas, Canvas, Blocks, Tasks, or Notes were built — those start in Phase 2.
- Fixed post-review: Escape now closes only the topmost popup (module-level open-order stack in `Popup.tsx`), and the backdrop only closes on a click where both mousedown and mouseup land on the backdrop itself, not on a drag that starts inside the dialog and releases outside.

### Phase 2 — what was built

- `src/types.ts` — `Area` row type, mirroring `supabase/migrations/0001_init.sql`; grows as later phases add Blocks, Tasks, and Notes.
- `src/lib/areas.ts` — TanStack Query hooks (`useAreas`, `useCreateArea`, `useUpdateArea`, `useDeleteArea`) wrapping the Supabase `areas` table. Delete only removes the `areas` row — Blocks and Notes underneath are left to the migration's `on delete cascade`, per the instruction not to hand-delete children in client code.
- `src/components/AreaTabs.tsx` — the tab row: one tab per Area ordered by `created_at` (query already orders this way), click to activate via `uiStore.setActiveAreaId`, "+" opens the create popup, double-click opens the edit popup. A `useEffect` keeps `activeAreaId` valid, falling back to the first remaining Area (or `null`) whenever the active one disappears — covers both first load and post-delete.
- `src/components/AreaCreatePopup.tsx` / `AreaEditPopup.tsx` — both reuse `Popup`. Create is a single name field. Edit has the name field plus Delete, which awaits `useConfirm()` before calling the delete mutation — no instant deletes.
- `src/App.tsx` — the placeholder "Signed in as…" screen is now a real shell: `AreaTabs` plus sign-out in a header, and a content area below that names the active Area (a stand-in for the Phase 3 Canvas) or shows an empty-state message when there are no Areas at all.
- No new dependencies. Popup and `useConfirm()` reused as-is, unmodified.
- **2.4's acceptance criterion is unverified**: no live Supabase project/credentials in this environment, and Blocks/Notes don't exist yet to actually test the cascade. What's implemented is the client side only — delete the `areas` row and trust the migration's `on delete cascade` — matching the instruction not to hand-delete children. Confirming "no orphaned rows" needs a live project and, practically, Blocks/Notes from Phase 3 to have something to orphan.
- Verified in a headless browser against an in-memory mock of the Supabase client (temporary, not committed — restored from the real client before pushing, same pattern as Phase 1's temporary test buttons): starting from zero Areas, the empty state renders; "+" opens the create popup, submitting adds a tab and makes it active; a second Area is created and both tabs render in creation order; clicking a tab switches the active one; double-clicking a tab opens the edit popup pre-filled with its name and a Delete action; renaming through the edit popup updates the tab; clicking Delete opens the confirm dialog naming the Area, Cancel leaves the edit popup open and the Area intact, Confirm deletes it; after deleting the active Area the remaining one becomes active; deleting the last Area returns to the empty state. No console or page errors during any of this. Not exercised: the actual RLS-backed Supabase network calls, and the cascade to Blocks/Notes (per above).

### Phase 3 — what was built

- `src/types.ts` — `Block` and `Task` row types added, mirroring `supabase/migrations/0001_init.sql` (untouched — no schema changes this phase).
- `src/lib/blocks.ts` — `useBlocks(areaId)`, `useCreateBlock`, `useUpdateBlockPosition`, same TanStack Query pattern as `src/lib/areas.ts`. `useCreateBlock` inserts the Block and, if any quick-capture lines were typed, the Tasks in the same mutation. No hook for status or name edits, or for task CRUD — that's Phase 4's Block popup, not built here.
- `src/components/Canvas.tsx` — fills the space below the tab row (3.1): a small non-scrolling toolbar holding "+ Add block", and a `relative overflow-auto` div below it that renders one absolutely-positioned `BlockCard` per Block at its stored `x`/`y`. No zoom or pan; scrolling is the container's native scroll if blocks land past the viewport. "+ Add block" reads the scroll container's current viewport (`scrollLeft/Top` + `clientWidth/Height`) to compute the centre position passed to the create popup (3.5) — a one-time coordinate, not an arrangement rule; nothing moves a block after creation.
- `src/components/BlockCard.tsx` — the draggable block (3.2), plain pointer events (`onPointerDown`/`onPointerMove`/`onPointerUp` with `setPointerCapture`), no drag-and-drop library. Position is local state while dragging and is written back via `useUpdateBlockPosition` on pointer-up; freeform, no snapping/grid/auto-stacking. Also renders the three status treatments (3.4): `active` full colour (`bg-blue-500`), `done` dimmed (`grayscale` + muted gray), `upcoming` the same dimmed classes plus a small `absolute` coloured dot so it doesn't read as `done` at a glance.
- `src/components/BlockCreatePopup.tsx` — opened by "+ Add block" (3.3), reuses `Popup`. A name field plus a plain `<textarea>` for quick-capture tasks — typing Enter in a textarea already produces the next line for free, so no custom key handling was needed; on submit each non-empty line becomes a Task row, and an empty textarea creates the Block with no tasks (tasks are optional, per spec).
- `src/App.tsx` — `Workspace` now renders `<Canvas areaId={activeArea.id} />` in place of the Phase 2 placeholder text; switched the outer layout from `min-h-screen` to `h-screen` so the Canvas's `flex-1 overflow-auto` has a bounded height to scroll within, instead of the whole page growing.
- No new dependencies (per the phase's "no drag-and-drop library" instruction). Blocks have no delete in this phase — that's Phase 4. Block status can only be `upcoming` (the schema default) through anything built here; setting it to `active`/`done` is also Phase 4's Block popup.
- Verified in a headless browser against a temporary in-memory mock of the Supabase client (same pattern as Phase 1/2 — written over `src/lib/supabase.ts` and a one-line temporary `window.__queryClient` exposure in `src/main.tsx` for the session, both fully reverted and diffed clean before committing): created an Area, then "+ Add block" opened the popup; creating a block with three quick-capture lines produced 3 task rows in the mock store; creating a second block with the tasks field left empty produced a block with 0 tasks and left the task count unchanged — confirming tasks are optional. Dragged the first block with simulated pointer movement (150px/120px delta): the block moved on screen by exactly that delta and the mock store's row for it was mutated to the new `x`/`y` — confirming drag-drop persists through the data layer, not just local component state. Seeded three additional rows directly in the mock store with `status` `active`/`done`/`upcoming` and forced a query refetch (not a full page reload, which would have wiped the in-memory mock): the `active` block rendered with the blue "full colour" classes and no marker; `done` and `upcoming` rendered with identical dimmed/grayscale classes; only `upcoming` additionally rendered the small coloured marker dot — confirmed both via computed-style comparison and a screenshot. No console or page errors during any of this. **Not verified**: anything against a live Supabase project/database — this environment has no credentials, per the instruction not to seek any. RLS, real network latency, and real Postgres constraints (e.g. the `status` check constraint) were not exercised.
- **Post-review fix**: removed the redundant `grayscale` Tailwind class from `statusClasses.done`/`.upcoming` in `BlockCard.tsx` — it desaturated the nested yellow marker along with the card body, making `upcoming` read as grey and defeating 3.4; `bg-gray-300 text-gray-500` alone still gives the dimmed look. Also made `handlePointerUp`'s position mutation conditional on `x`/`y` actually differing from `block.x`/`block.y`, so a zero-distance click no longer writes to the database. Verified in a headless browser (temporary harness rendering `BlockCard` directly, reverted before commit): the `upcoming` marker now renders as actual yellow (computed `oklch(0.852 0.199 91.936)`, matching `bg-yellow-400`) against a grey card, `done`/`upcoming` still share identical computed background/filter, a click with no pointer movement fired zero requests, and a drag persisted the correct new coordinates in one request.

### Phase 4 — what was built

- `src/lib/tasks.ts` — `useTasks(blockId)`, `useCreateTask`, `useUpdateTask`, `useDeleteTask`, same TanStack Query pattern as `src/lib/areas.ts`/`src/lib/blocks.ts`. Always read as a flat, unordered list (`order('created_at', ...)` for stable rendering only — no ordering column exists or is exposed). `useUpdateTask` covers both inline text edits and the complete-toggle, since both are just a partial update to the same row.
- `src/lib/blocks.ts` — added `useUpdateBlock` (name and/or status) and `useDeleteBlock`. Status is only ever written here, from an explicit user action; nothing reads task completion to decide it. `useDeleteBlock` only removes the `blocks` row — its Tasks are left to the migration's `on delete cascade`, same reasoning as Area delete not hand-deleting Blocks/Notes.
- `src/components/BlockEditPopup.tsx` — the one popup for 4.1-4.5, opened by double-clicking a Block. Name is a single input that commits on blur or Enter (no Save button — the popup stays open afterward so status and tasks can keep being edited in the same session, unlike `AreaEditPopup` which closes on submit). Status is three plain buttons (Upcoming/Active/Done), the current one highlighted; clicking one is the only way it ever changes. Tasks render as a flat list (`TaskRow`, a local component in the same file) — checkbox to toggle complete, an inline text input that commits on blur/Enter, and a Delete button gated by `useConfirm()`. An "Add a task" form at the bottom of the list appends new ones; there is no drag handle, order control, or up/down affordance anywhere in the list. Delete block is gated by `useConfirm()` too, and closes the popup on confirm.
- `src/components/BlockCard.tsx` — added double-click-to-open without touching the existing pointer-based drag (3.2). Native `dblclick` wasn't used: `setPointerCapture` keeps every pointer event routed to the block regardless of how far the cursor actually travelled, so the browser would still fire a `dblclick` after two real drags done in quick succession over the same block. Instead, `handlePointerUp` measures the distance travelled since `pointerdown`: over a small threshold (4px) it's a drag — position is persisted as before, and a click-timer ref is cleared so it can never combine with anything into a double-click; at or under the threshold it's a click, and if a prior click landed within 400ms it counts as a double-click and opens `block-edit` via the `uiStore` `openPopup`/`popupContext` pattern (same as `AreaTabs`/`Canvas` already use). A real drag can now never open the popup, and a double-click (near-zero movement on both clicks) can never displace the block, since the existing "only write x/y if it actually changed" guard from the Phase 3 post-review fix still applies to the drag path.
- `src/components/Canvas.tsx` — renders `BlockEditPopup` when `activePopup === 'block-edit'`, reading `blockId`/`areaId` out of `popupContext`, same shape as the existing `block-create` wiring.
- No new dependencies. No schema changes — the `tasks` table already had everything 4.1-4.5 needed; nothing here needed an ordering column, and none was added.
- Verified in a headless browser against a temporary in-memory mock of the Supabase client (written over `src/lib/supabase.ts`, restored before committing, same pattern as every prior phase) seeded with one Area, one Block, and two Tasks (one already complete): a real drag (mouse down, move 100px/80px, up) moved the block and did not open the popup; a double-click (two zero-movement clicks under 400ms apart) opened it without moving the block; renaming via the input and blurring persisted the new name (confirmed the input still showed it, and it survived reopening the popup); clicking Active then Done then Upcoming each re-highlighted the corresponding button and correspondingly changed the card's background behind the popup (blue for Active, dimmed grey for Done/Upcoming, matching 3.4); with status set to Active, checking both existing tasks' checkboxes complete left Active still highlighted — no automatic move to Done; typing into "Add a task" and pressing Enter appended a third task row; editing that row's text and blurring persisted the new text; toggling its checkbox checked it and applied the strikethrough style; clicking a task's Delete opened the confirm dialog naming that task, Cancel left it in the list, Delete-then-Confirm removed it; clicking "Delete block" opened the confirm dialog naming the block and mentioning its tasks, Cancel left the popup open with the block intact, Delete-then-Confirm closed the popup and removed the block from the canvas (now empty, matching the mock's cascade of its tasks). No console or page errors during any of this. **Not verified**: anything against a live Supabase project — this environment has no credentials, per the instruction not to seek any.

### Phase 5 — what was built

- `src/types.ts` — `Note` row type added, mirroring `supabase/migrations/0001_init.sql` (untouched — no schema changes this phase; the table already had everything needed).
- `src/lib/notes.ts` — `useNotes(areaId)`, `useCreateNote`, `useUpdateNotePosition`, `useUpdateNoteContent`, `useDeleteNote`, same TanStack Query pattern as `src/lib/blocks.ts`. `useCreateNote` inserts an empty-content row immediately (5.1 — no popup). Position and content are separate mutations, mirroring how Blocks split position (`useUpdateBlockPosition`) from other edits (`useUpdateBlock`). `useDeleteNote` is the one mutation behind both the silent 5.3 auto-discard and the explicit 5.4 delete — the two paths differ only in whether a caller awaited `useConfirm()` first.
- `src/components/NoteCard.tsx` — the draggable note. Drag is plain pointer events on a small header strip only (not the whole card, unlike `BlockCard`), so the textarea underneath keeps normal text-editing pointer behaviour (click-to-place-cursor, selection) instead of fighting the drag handlers; position persists on pointer-up exactly like `BlockCard`. Content is local state, saved via `useUpdateNoteContent` on blur.
  - **The 5.3-vs-5.4 distinction** (the one open decision this phase resolved, see below): tracked with a `useRef<boolean>` per note, `hadContentRef`, initialised from whether the note already has content on mount and flipped permanently to `true` the first time the textarea's content becomes non-empty — it never flips back. On blur: content empty **and** `hadContentRef.current` still `false` → the note was never given real content, so it's deleted immediately with no confirm (5.3). Anything else — content present, or content now empty but `hadContentRef.current` is `true` — just saves, confirm-gated delete is the only way to remove it (5.4). This is exactly the client-state substitute for a "was ever non-empty" column, which the schema deliberately doesn't have.
  - Explicit delete (5.4) is a small "Delete" button in the header strip, gated by `useConfirm()` like every other delete in the app. It sits inside the same header as the drag handle, which surfaced a real bug during verification: the header's `setPointerCapture` (copied from `BlockCard`'s pattern) retargets *all* subsequent events for that pointer to the capturing element, including the button's own `click` — so clicking Delete never fired its handler at all, silently. Fixed by skipping `setPointerCapture` (and the whole drag) when the pointerdown's target is inside a `<button>`. Also guarded: a mousedown on Delete sets a `deletingRef` flag before the textarea's blur fires, so a click on Delete can never race with the 5.3 auto-discard path on the same note; if the confirm is cancelled, any pending content edit is saved instead of silently dropped.
- `src/components/Canvas.tsx` — added a "+ Note" button next to "+ Add block" in the toolbar. No popup: it calls `useCreateNote` directly, positions the note at the current viewport centre (same one-time-coordinate approach as 3.5, not an arrangement rule), and remembers the new note's id so its `NoteCard` renders with `autoFocus` on mount (5.1's "focused for typing"). Renders one `NoteCard` per Note from `useNotes(areaId)` alongside the existing `BlockCard`s (5.5 — both live on the same canvas, unrelated to each other).
- No new dependencies. No schema changes — the `notes` table already had everything 5.1-5.5 needed.
- Verified in a headless browser against a temporary in-memory mock of the Supabase client (written over `src/lib/supabase.ts`, restored before committing, same pattern as every prior phase), seeded with one Area: clicking "+ Note" created a row immediately (confirmed against the mock store, no popup ever rendered) and `document.activeElement` was the new note's `<textarea>`; typing "Hello world" and blurring (a click elsewhere) persisted that content to the mock store; dragging the header handle by (150, 100) moved the card on screen and persisted the new `x`/`y`; creating a second note and blurring it immediately with no typing removed it from the store entirely (5.3); creating a third note, typing "Temporary content", then selecting-all and deleting the text before blurring left the row in the store with `content: ""` — it survived (5.4, the critical case); creating a fourth note with real content and clicking its Delete button opened the confirm dialog ("Delete this note?"); Cancel left the note and its content untouched, and a subsequent Delete-then-Confirm removed it from the store. All of this was also screenshotted and visually inspected, not just asserted from store state — notes render as a small yellow card with a drag-handle/Delete header above the text area, multiple notes coexist and are independently draggable, and the confirm dialog overlays correctly. No console or page errors during any of this. **Not verified**: anything against a live Supabase project — this environment has no credentials, per the instruction not to seek any.
- **Post-review fix** (caught during this phase's own verification, not a later pass): the pointer-capture-swallows-the-Delete-click bug described above. No other issues found.

## V2 — what was built

Phase numbers in this section are v2's own, from `TASKS.md`, restarting at 1 — see the note under "V1 — what was built" above for why that matters.

### Phase 1 — what was built

- `src/components/AutoGrowTextarea.tsx` — the one auto-growing text component (1.1). A thin wrapper around a plain `<textarea>`: `rows={1}` for the smallest possible resting size, `resize-none overflow-hidden` so there's never a manual resize handle or an inner scrollbar, and a `useLayoutEffect` keyed on `value` that resets `style.height` to `'auto'` before reading `scrollHeight` and writing it back. Resetting to `'auto'` first (rather than only ever growing) is what makes it shrink back down when content is removed, not just grow — the height is recomputed from scratch on every keystroke, in either direction.
- `src/components/NoteCard.tsx` — swapped the fixed `h-24` textarea for `AutoGrowTextarea` (1.2), dropping the fixed height and the `flex-1` that used to stretch it; the note's drag/position/content-save/auto-discard logic (v1 Phase 5) is untouched, since this phase only changes how the box is sized, not what it saves or when.
- `src/components/BlockEditPopup.tsx` — task text now uses `AutoGrowTextarea` in both places named in the spec (1.3): `TaskRow`'s inline edit field, and the "Add a task" field at the bottom of the list. Both were plain `<input>`s before; moving to a textarea is also what makes multi-paragraph task text possible at all; a checkbox row's `items-center` became `items-start` so the checkbox stays aligned to the first line instead of centering against a now-taller box. This changes keyboard behavior deliberately: Enter now inserts a newline in both fields instead of committing (`TaskRow`) or submitting the form (Add-a-task) — a single-line `<input>`'s implicit Enter-submits behavior doesn't exist on `<textarea>` to begin with, so no code was needed to suppress it. `TaskRow` still commits on blur, same as before; "Add a task" now only submits via its button, no longer via Enter. The Block name field (a single-line `<input>`, not named in the spec) is untouched, Enter-blurs as before.
- No new dependencies, no schema changes, no other fields touched — Area names, Block names, and the quick-capture bulk-entry textarea in `BlockCreatePopup` (a distinct line-splitting mechanism, not a single task's text field) are exactly as v1 left them, per the spec naming only Task text and Note text.
- Verified in a headless browser against a temporary in-memory mock of the Supabase client (written over `src/lib/supabase.ts`, restored before committing, same pattern as every v1 phase; Playwright installed with `npm install --no-save` for the session and removed afterward, since it isn't a project dependency): typed three long paragraphs into a Note and into an existing task's inline field — both grew from a 28-32px single-line box to roughly 270-350px, `scrollHeight` matched `clientHeight` in both (`overflow-y: hidden`, no inner scrollbar), and a screenshot of each confirms every paragraph fully visible with nothing clipped. Clearing the content back out returned both to their original single-line height. In the "Add a task" field, typing a line, pressing Enter, and typing a second line grew the box and left a real `\n` in the field's value instead of submitting early; clicking Add then submitted it, cleared the field, and shrank it back down. No console or page errors during any of this.
- **Fix chunk, 2026-09-11 — two defects found and closed** (measurements in the build log below):
  - `AutoGrowTextarea.tsx` was short by its own border width on any bordered instance. Tailwind's preflight sets `box-sizing: border-box`, so `style.height` sets the border box while `scrollHeight` describes the content box — the gap between them is exactly the border width, invisible on the Note field (no border) and real on the bordered task fields here. Fixed by reading `el.offsetHeight - el.clientHeight` right after the `height: 'auto'` reset and adding it onto `scrollHeight` before writing the final height. Still a border box — nothing about the Note field's own sizing changed, since the added term is 0 there.
  - `BlockEditPopup.tsx`'s "Add a task" field: Enter now submits the form (adds the task) again, and Shift+Enter inserts a newline — restoring the Enter-adds behaviour the `<input>`→`<textarea>` swap above had silently dropped, without giving up multi-line entry. An `onKeyDown` calls `event.currentTarget.form?.requestSubmit()` on a plain Enter and does nothing on Shift+Enter, letting the browser insert the newline itself; the Add button's own submit path is untouched. `TaskRow`'s inline edit field is unchanged — Enter still inserts a newline there and it still commits on blur, per 1.3's own multi-paragraph acceptance criterion.

### Phase 2a — what was built

Schema, RLS and client compatibility, written and proven against a scratch database only. **The live project is untouched** — that is Phase 2b, gated on a confirmed backup, not started.

- `supabase/migrations/0002_v2.sql` (2a.1) — exactly the Data model delta from `TASKS.md`, in order: `lists`, `tasks.list_id` (added, backfilled, then `tasks.block_id` dropped), `notes.block_id`/`notes_one_parent`, `blocks.focused`, `connections`, `waiting_entries`, `waiting_entry_tasks`, then RLS (2a.2) on the four new tables in the same file, same owner-UID-pinned "for all" shape as `0001_init.sql`, copied from that file's already-substituted UUID. The data migration runs once and is not idempotent, by design — a second run would create a second Flat list per block and re-home its tasks into it.
- `src/lib/lists.ts` (new) — `useLists(blockId)`, `useCreateList()`. Deliberately thin: no update or delete, since 2a.3 builds no list CRUD UI — the only writers are quick-capture and `BlockEditPopup`'s lazy "create the block's one Flat list on its first task" path, both at a fixed `(24, 24)` origin matching 2a.1's own data migration.
- `src/lib/tasks.ts` — `useTasks`/`useCreateTask`/`useUpdateTask`/`useDeleteTask` rekeyed from `blockId` to `listId` throughout, mirroring `tasks.list_id` replacing `tasks.block_id`.
- `src/lib/notes.ts` — a `NoteParent` union (`{areaId}` or `{blockId}`) and a `noteParent(note)` helper replace the single `areaId` every hook took; all five hooks (`useNotes`, `useCreateNote`, `useUpdateNotePosition`, `useUpdateNoteContent`, `useDeleteNote`) take a `parent` instead. No new UI surface for block-level notes — `Canvas.tsx` is the only caller today and it still only ever passes `{ areaId }`; Phase 3's project canvas is what will pass `{ blockId }` for the first time.
- `src/lib/blocks.ts` — `useCreateBlock` now creates a Flat list (when quick-capture has any lines) before inserting the tasks into it, instead of inserting tasks straight onto the block; an empty quick-capture still creates no list, matching a block with no tasks getting no list.
- `src/components/BlockEditPopup.tsx` — reads `useLists(blockId)` and treats `lists[0]` as "the" block's list (this stepping stone never produces more than one); `useTasks(list?.id ?? null)` follows from that. Adding a task when no list exists yet lazily creates one first, at the same fixed origin, then adds the task into it — the interactive-path twin of 2a.1's own migration and `useCreateBlock`'s quick-capture path; all three agree on where a block's implicit Flat list comes from.
- `src/types.ts` — grown to mirror `0002_v2.sql` in full: `List`, `Connection`, `WaitingEntry`, `WaitingEntryTask` added; `Task.block_id` replaced by `Task.list_id`; `Note.area_id` widened to nullable and `Note.block_id` added; `Block.focused` added. Only `List` and the `Task`/`Note`/`Block` changes are used by any hook this chunk — `Connection`/`WaitingEntry`/`WaitingEntryTask` are unused mirrors, following the file's own stated pattern of tracking the whole schema.
- No project canvas, list CRUD UI, kind picker, Waiting, or connections were built — the visible app behaves exactly as it did under v1's shape, per 2a.3's own boundary.
- **2a.1 verified 2026-09-11** by a fresh run (not the planning session's earlier one) against a scratch Postgres 16 seeded with two Areas, three Blocks (one with three tasks including one completed, one with zero tasks, one in a second Area with two tasks including one completed), and two Area-level Notes: all 5 tasks survived under their original block in a new `kind='flat'` list with `text`/`completed` unchanged; the zero-task block got zero lists; `tasks.block_id` is gone from the table. Also exercised: `notes_one_parent`, `lists.kind`, `waiting_entries.kind`, the connections self-edge/duplicate/2-cycle behavior, and the full cascade chain from an Area delete through lists, tasks, notes at both levels, connections, and waiting entries/picks — see `TASKS.md` 2a.1 for the exact counts.
- **2a.2 verified 2026-09-11, honestly short of the real thing.** This container has no Supabase CLI, no PostgREST, no GoTrue — confirmed, not assumed: `docker pull postgrest/postgrest` was attempted and blocked by the environment's egress allowlist (`Forbidden` on the Docker Hub CDN blob). `npm run verify:rls` was extended to all eight tables and is verified by reading only; it has not run anywhere in this chunk. In its place, a minimal `auth.uid()` stub plus `anon`/`authenticated` roles were built directly in the scratch database, and all eight tables were probed as `anon` with no `request.jwt.claims` and with claims naming a different uid: every `select` returned 0 rows, every `insert` failed with SQLSTATE `42501` (confirmed literally, via a `DO` block — the same code PostgREST surfaces), every `update`/`delete` affected 0 rows. `verify:rls`'s own run against PostgREST is still owed, at 2b, against the live project.
- **2a.3 verified 2026-09-11** in Chromium against `vite dev` (Playwright, `npm install --no-save`, removed after) — real hooks, real mutation paths, no mocked-over `supabase.ts`. Rather than a hand-written fixture, `/rest/v1/*` was intercepted by a hand-rolled PostgREST stand-in that forwards every request as real SQL to the same scratch Postgres 2a.1 was proven against, so this chunk's client code ran against the actual migrated shape; `/auth/v1/*` was stubbed and a session seeded into `localStorage`, same pattern as the Phase 1 fix chunk. 20/20 checks passed, all asserted against the scratch database directly: create/rename/delete Area; create a Block via quick-capture (confirmed exactly one Flat list at `(24,24)` holding both lines) and a Block with none (confirmed zero lists); the block popup's full task CRUD (add, toggle, inline edit, delete, all in the same list — no second list ever created) plus block delete cascading the list and remaining tasks; the lazy list-creation path on a previously listless block, confirmed to create exactly one Flat list at the same fixed origin; Note create/type/blur persisting with `area_id` set and `block_id` null; Area delete cascading its Block and Note. Two screenshots taken. One test-script false failure was found and fixed in the test, not the app: two quick-captured tasks inserted in one statement share a single `created_at`, so ordering between them is a genuine, expected tie (this app's tasks are deliberately unordered, no ordering column) — the check now keys off the row a checkbox actually targets instead of re-deriving order from a tied sort. Not checked: step-list ordering (doesn't exist until Phase 3) and the live project (no credentials here, owed at 2b).

### Phase 3 — what was built

What a Block contains, replaced. No schema change — `0002_v2.sql` already created `lists`, `tasks.list_id`, and `notes.block_id`; this phase is entirely client-side, and per the incident recorded in Phase 2's build-log entries, no file was added to `supabase/migrations/`.

- `src/components/Popup.tsx` — grew two optional props, `contentClassName`/`contentStyle`, so one caller can replace the content box's own sizing without touching the shared backdrop/portal/focus-trap/Escape-stack behaviour every popup relies on. Every existing caller (`AreaCreatePopup`, `AreaEditPopup`, `BlockCreatePopup`, `ConfirmDialogProvider`, the new `ListCreatePopup`) passes neither and is byte-identical in output to before.
- `src/components/BlockEditPopup.tsx` (3.1, 3.2, 3.7) — rewritten from a flat task list into the project canvas host. The popup is now resizable (3.1) via native CSS `resize: both` on `Popup`'s content box (no library) — an explicit starting size (640×520), a floor (380×320), and a ceiling (95vw/90vh); nothing about a resized size is written anywhere, so it resets every time the popup is reopened, per spec ("resizable", not "remembered"). Below a fixed header (name/status/"+ Add list"/"+ Note"/"Delete block", unchanged from Phase 4 except the delete-block warning now also names lists and notes), the body is a `relative overflow-auto` canvas (3.2) rendering one `ListCard` per List and one `NoteCard` per Note at their stored `x`/`y` — the same placement model as `Canvas.tsx`'s Blocks, one level down, and no zoom/pan. "+ Note" calls `useCreateNote({ blockId })` directly (5.1's no-popup pattern, reused) — the first real caller of the `{blockId}` half of 2a.3's `NoteParent` union; `NoteCard` itself was not touched, let alone forked.
- `src/lib/lists.ts` — grew `useUpdateListPosition` (drag persistence) and `useDeleteList` (3.8, removes only the `lists` row — its tasks go with it via `on delete cascade`, not a hand-written delete). `useLists`/`useCreateList` from 2a.3 are unchanged. There is still no `useUpdateList` for `kind` or `title` — kind is fixed at creation by the spec, and nothing asks for title to be editable either, so no such path was built.
- `src/components/ListCreatePopup.tsx` (new, 3.3) — kind (Step-by-step/Flat, Flat default) and an optional title, chosen once at creation. No later UI anywhere reads or writes `lists.kind` after the insert, which is what actually makes kind fixed — not a disabled control that a future change could accidentally re-enable.
- `src/components/ListCard.tsx` (new, 3.2, 3.4, 3.5, 3.6, 3.8) — the draggable List. Drag is confined to a header strip via plain pointer events with `setPointerCapture`, the same approach `NoteCard` already uses (and for the same reason: the task fields underneath need normal text-editing pointer behaviour). Holds its own `useTasks(list.id)` and full task CRUD (3.4): add, inline-edit, toggle, delete. Step-by-step gating (3.6) is two pure functions over the already-ordered (`created_at`) task array — `canComplete(index)` requires the previous task done; `canUncomplete(index)` requires no *later* task still done — applied only when `list.kind === 'step'`, wired to each checkbox's `disabled` attribute (not a click-time no-op) with a `title` explaining the refusal; a Flat list never disables anything. Task delete (3.5) calls `findWaitingHolds` before the confirm dialog and, if the task is held, folds the waiting project's name into the dialog's message; on confirm, deletes the task (whose own pick rows cascade away via `waiting_entry_tasks.task_id on delete cascade`) and then removes any entry that pick was the last one in. List delete (3.8) is confirm-gated and removes only the `lists` row.
- `src/lib/waiting.ts` (new) — `findWaitingHolds(taskId)` (two-step read: which entries pick this task, then those entries' block names and full pick counts, since PostgREST embeds a to-one `blocks(name)` relation but not a "how many other picks does this entry have" count) and `removeEmptiedWaitingEntries(holds)`. Nothing in the app calls these except `ListCard`'s task-delete path — there is no Waiting UI yet, so every call this phase returns `[]` against real data; the browser verification below seeded rows directly by SQL to exercise the non-empty path, since Phase 4 hasn't shipped the only path that would populate them for real.
- No changes to `src/lib/tasks.ts`, `src/lib/notes.ts`, `src/lib/blocks.ts`, `src/types.ts`, or `NoteCard.tsx` — everything Phase 3 needed from those was already there from 2a.3.
- **The known `BlockEditPopup.handleAddTask` create-path race (flagged, not fixed, in 2a's review) is closed by construction.** That path — lazily creating a block's one implicit list the first time a task was added, with a retry-before-refetch window that could create a second — no longer exists in any form. A task can now only be added to a `ListCard` for a List that already has a real, persisted `id` (the user created it explicitly via "+ Add list" first), so there is no lazy list-creation left on the interactive add-task flow to race. `useCreateBlock`'s quick-capture path (unaffected, untouched this phase) was never exposed to this defect in the first place — a failed retry there creates a whole new Block, not a second list under an existing one.
- **Verified 2026-09-11** in headless Chromium (Playwright, `npm install --no-save`, removed after) driving the real `vite dev` app — unminified, real hooks, real mutation paths, no mocked-over `supabase.ts` — against a scratch Postgres 16 with `0001_init.sql`, `0002_v2.sql`, and `0003_v1_backup_snapshot.sql` applied and a stub `auth.uid()` reading `request.jwt.claims`. Unlike a mock, every write went through a hand-rolled relay that ran real parameterized SQL, inside `SET LOCAL ROLE authenticated` with the owner UUID set as the JWT claim — genuine RLS enforcement, not a superuser connection standing in for it (confirmed separately: the same relay refused a write carrying no claim with the real `42501`/"new row violates row-level security policy" error, before the harness's real checks began). 33/33 checks passed, all asserted against the scratch database directly, not the DOM:
  - **3.1** `getComputedStyle(popupEl).resize === 'both'`; dragging the native resize handle by (120, 80) grew `offsetWidth`/`offsetHeight` by more than 50px each, measured before/after.
  - **3.2/3.3** created a Step-by-step list ("Setup steps") and a Flat list ("Errands"), each with its title and kind confirmed by direct query; dragging the step list by its header persisted a new `lists.x`/`y`.
  - **3.4/3.6** the exact four-step sequence the plan's acceptance bar names, against a real 3-task step list: completing task 2 while task 1 was incomplete was refused (checkbox `disabled`, a forced click left `completed=false` in the database); completing 1 then 2 succeeded; un-completing 1 while 2 was complete was refused (only the *last* completed task's checkbox was enabled); un-completing 2 then 1 succeeded. The same operations against a 2-task Flat list in the same project refused nothing in either direction.
  - **3.7** a project Note persisted with `block_id` set and `area_id` null; a second, never-typed-into Note was auto-discarded with no net row increase.
  - **3.5** seeded a second block ("Other Project") and a `waiting_entries`/`waiting_entry_tasks` row picking one of the step list's tasks directly by SQL (Phase 4 has no UI for this yet), then deleted that task through the UI: the confirm dialog's message contained "Other Project" alongside the ordinary warning; after confirming, the task, its pick, and — the part that needed real code — the now-empty `waiting_entries` row were all gone, with no second confirm dialog.
  - **3.8** deleted the Flat list (2 tasks) through its own confirm dialog: the `lists` row and both `tasks` rows were gone, the tasks via cascade rather than a hand-written delete. Also checked beyond the acceptance line: deleting the block itself cascaded its remaining list, that list's remaining tasks, and its note in one action.
  - Two screenshots taken (the resized popup holding both list kinds, a note, and a second block visible on the Area canvas behind it; a closer view of the step list mid-verification). Zero browser console errors across the entire run.
  - **Not checked:** the live Supabase project (this container had no `.env` at session start, by design); `verify:rls` (no schema change this phase, so nothing new to verify there); anything Phase 4/5/6/7 would add (Waiting UI, Focused, Connect, Filter) — out of scope, per the spec.

### Phase 5 — what was built

Focused status, its mutual exclusion with Waiting, and the Focus screen. No schema change — `blocks.focused` already existed from `0002_v2.sql`; nothing was added to `supabase/migrations/`.

- `src/lib/blocks.ts` — `useUpdateBlock`'s mutation type grew `focused?: boolean` alongside `name`/`status` (it already spread its remaining fields generically into the `update()` call, so the write path needed no other change). New `useFocusedBlocks()`: every `focused = true` block across every Area — not scoped to the active one, since that's the Focus screen's whole reason to exist — read via the `blocks -> areas(name)` FK embed, the same PostgREST-embed pattern `src/lib/waiting.ts` already used for `blocks(name)`. `useUpdateBlock`/`useDeleteBlock` both invalidate that query's key (`['focused-blocks']`) alongside their existing per-Area one, so toggling or deleting a block anywhere keeps the Focus screen's list correct without a manual refetch.
- `src/components/BlockEditPopup.tsx` — a Focus/Focused toggle button (5.1), and the two mutual-exclusion refusals (5.2): `handleToggleFocused` refuses turning Focused on when `isProjectWaiting(entries)` (4.1's own helper, reused) is true; `handleOpenWaitingPopup` refuses opening the "+ Waiting entry" popup — before it ever renders, not after a wasted fill-in — when `entries.length === 0 && block.focused`, since adding a project's *first* entry is exactly "turning Waiting on" and once a project already has entries it was never Focused to begin with (this same guard already refused that transition on the way in). Both refuse via the existing `pushError` mechanism (4.5's own pattern), naming the other state by name rather than silently swapping or discarding anything. `handleStatusChange` folds `focused: false` into the *same* `useUpdateBlock` call when a project reaches `done` while still Focused (5.3), rather than a second mutation a failure could leave half-applied.
- `src/components/BlockCard.tsx` — a real CSS `outline` (`outline outline-2 outline-offset-2 outline-purple-600`) when `block.focused` (5.4), deliberately not a `ring`/box-shadow approximation — the spec's own word is "outline", and 5.2's mutual exclusion means a block is never asked to carry both this and Waiting's dimmed-colour/Ready-badge treatment at once, so there was no layering conflict to design around.
- `src/components/FocusScreen.tsx` (new) — the Focus screen (5.5). Reads `useFocusedBlocks()`, groups by `area_id`, and renders one button per project under its Area's heading. Clicking one calls `setActiveAreaId`, `setScrollTarget` (a new one-shot `uiStore` field), `openPopup('block-edit', ...)`, and `setActiveView('canvas')` together — the Area switch, the popup, and the scroll request all fire from the same click, not a multi-step flow.
- `src/store/uiStore.ts` — `activeView: 'canvas' | 'focus'` (which top-level screen `Workspace` renders) and `scrollToBlockId`/`setScrollTarget`/`clearScrollTarget` (the one-shot scroll-into-view signal `Canvas.tsx` consumes). Neither is persisted — transient UI state only, same as everything else already in this store.
- `src/components/Canvas.tsx` — a `useEffect` watching `scrollToBlockId`: once the target block is actually present in the now-active Area's loaded `blocks` (not merely once the effect fires — a fresh Area's query may still be loading, so the effect is written to retry itself across `blocks` updates rather than give up on a not-yet-there block), it calls `scrollRef.current.scrollTo(...)` centred on the block's stored `x`/`y` and clears the target.
- `src/App.tsx` — a "Focus screen" toggle button in the header, next to the Area tabs, swapping the Workspace body between `Canvas` and `FocusScreen`. Named "Focus screen" rather than "Focus" specifically to stay distinct from the in-popup toggle's own "Focus"/"Focused" label — the two are visually far apart and never confusable to a person, but sharing an accessible name would have made them indistinguishable to anything (a screen reader, an accessibility-tree query) that isn't reading screen position, so the header button earned the more explicit label rather than the popup's shorter one losing clarity for symmetry.
- `src/components/AreaTabs.tsx` — clicking an Area tab now also calls `setActiveView('canvas')`, so clicking a tab while on the Focus screen behaves the way a person would expect (it shows that Area's canvas) rather than leaving the Focus screen up with a different Area now "active" behind it.
- No new dependencies, no schema change — `blocks.focused` was already there from `0002_v2.sql`, unused by any client code until this phase.
- **Verified 2026-09-11** in headless Chromium (Playwright, `npm install --no-save`, removed after) driving the real `vite dev` app against a scratch Postgres 16 with `0001`+`0002`+`0003` applied and a stub `auth.uid()` reading `request.jwt.claims`; a hand-rolled relay translated `/rest/v1/*` into real parameterized SQL under `SET LOCAL ROLE authenticated` with the owner UUID as the JWT claim (genuine RLS, not a superuser bypass), throwing on any filter operator it didn't implement and never treating `select`/`order`/`limit`/`offset`/`columns`/`on_conflict` as a filter — both lessons taken directly from Phase 4's own review notes below. Seeded 2 Areas (Work: Alpha/Beta/Gamma/Epsilon; Home: Delta), with Beta pre-seeded holding one free-text Waiting entry and Gamma placed at `x=3000, y=2500` specifically to start off-screen. **29/29 checks passed**, every one asserted against the scratch database directly (a separate `pg` connection from the app's own bridge) or a computed DOM property, not just visible text:
  - **5.2, both directions, against real database state, not just a banner:** focusing Beta (Waiting) was refused — `blocks.focused` stayed `false` — with a banner naming Waiting; opening "+ Waiting entry" on a Focused, entry-free Epsilon was refused *before the popup rendered* (`text=New Waiting entry` count `0`) with a banner naming Focused, and `waiting_entries` for Epsilon stayed empty. Clearing each blocker then allowed the other: unfocusing Epsilon let a Waiting entry be created on it (row appeared, text confirmed by query); deleting Beta's Waiting entry via its own confirm dialog then let Beta be Focused (`focused` became `true`, confirmed by query).
  - **5.3:** Focused Alpha marked Done read `status = 'done', focused = false` from the database.
  - **5.5, the cross-Area jump proven as genuinely cross-Area:** with Work active, clicking Delta (Home) from the Focus screen switched the active Area tab to Home *and* opened a popup whose name field read `"Delta"` — the test deliberately made Work active first, since clicking a project already in the active Area wouldn't prove anything about the jump. With Home active, clicking Gamma (Work, seeded off-screen) switched back to Work and left the canvas's own scroll container at `scrollLeft: 1880, scrollTop: 1738` — far from its `(0,0)` rest position, which is what actually proves the scroll-into-view code ran rather than merely that Gamma was present somewhere on an overflowing canvas. Marking Gamma Done removed it from the Focus screen's list, checked *alongside* confirming Beta and Delta were still listed — specifically so "Gamma is absent" couldn't be a vacuous pass against an accidentally-emptied list.
  - **5.4, by computed style and screenshot, not appearance alone:** a Focused block (Delta) measured `outlineStyle: "solid", outlineWidth: "2px"`; a Waiting+Ready block built in the same database (Zeta — one picked-task entry, its task completed) measured `outlineStyle: "none"` and showed the `[aria-label="Ready"]` badge instead, with Delta showing zero such badges — the two states stay visually and structurally distinguishable side by side (screenshot: Delta blue with a purple outline beside Zeta's dimmed-blue Waiting card and green Ready check).
  - Zero browser console errors across the entire run. Two screenshots taken (Focus screen listing grouped by Area; canvas showing Focused-outline vs. Waiting+Ready-badge side by side).
  - One naming collision caught and fixed *during* this verification, not after: the header's Focus-screen toggle and the popup's own Focus/Focused toggle both read "Focus" in one state, making them indistinguishable by accessible name — fixed by renaming the header control to "Focus screen" (see `src/App.tsx` above), which is a real, if minor, UI-clarity improvement this verification earned rather than a test-only workaround.
  - **Not checked:** the live Supabase project (no `.env`/credentials at session start, by design — a placeholder `.env` was written for the `vite dev` verification run and deleted before finishing, the same pattern every phase since Phase 3 has used); Phase 7's Filter, which doesn't exist yet, so there was nothing to couple or accidentally coupled against; `npm run build` (201.72 kB with no `.env` present — consistent with the Phase 0/1/3 build-log entries on what that figure does and doesn't prove) and `npm run lint` both pass clean; no dependency added to `package.json`/`package-lock.json` (`playwright`/`pg` installed `--no-save` for the harness, uninstalled after, confirmed by an empty diff on both files); no `.env`, harness script, or scratch SQL committed.
- `TASKS.md` 5.1–5.5 ticked with acceptance notes recording the above. Phase 6 (Connect) is next, and is the first phase whose layout has to account for a block's real height — which, after Phase 4 and 5, can now carry a Waiting card and/or a Focused outline.

### Phase 6 — what was built

Block-to-block connections within an Area, acyclic, auto-laid-out by longest-path layering, with rigid whole-component dragging. The last feature phase. No schema change — `connections` already existed from `0002_v2.sql`; nothing was added to `supabase/migrations/`.

- `src/lib/layout.ts` (new) — pure graph functions, no Supabase/React, so the algorithm itself can be reasoned about independently of the data layer around it: `buildComponent` (undirected reachability through any connection, in either direction — 6.2/6.6/6.8's "component" and "orphan" both fall out of this one function), `wouldCreateCycle` (directed BFS from a candidate target checking whether it can already reach the candidate source — 6.3), and `computeConnectionLayout`, the layout algorithm itself (6.5/6.7). Its two branches: a *merge* of two previously separate components (the larger, by block count, is never touched — ties go to the upstream/source side; the smaller is rigidly translated, preserving its own existing shape, so its bridge endpoint lands one column-pitch from the winning side's, nudged a row if that slot is occupied); and an edge added *within* an already-single component, where 6.7 is silent — resolved conservatively (shift only the new edge's target and its downstream closure, only by the minimum needed, leaving everything upstream or on an unrelated branch untouched) and documented as a genuine spec-gap interpretation, not a product decision, in `TASKS.md`'s 6.5.
- `src/lib/connections.ts` (new) — `useConnections(areaId)`; `checkConnection` (6.3's self-edge, duplicate, and cycle refusals, called by the UI *before* any mutation, so a refusal never reaches the network and never writes a row); `useCreateConnection` (inserts the edge and whatever position updates `computeConnectionLayout` implies, in the same mutation, so a reload never shows one without the other); `useDeleteConnection` (deletes the row and writes no position, ever — 6.8); `useConnectBlocks`, the click-to-connect flow tying the above together for `BlockCard`'s use.
- `src/lib/blocks.ts` — `useUpdateBlockPosition` replaced by `useUpdateBlockPositions`, a batch write (one or more `{id, x, y}` rows in one mutation) so a connected block's whole component persists together (6.6); a lone block's drag is just a one-entry batch, unchanged in effect. `blocksKey` exported so `connections.ts` can invalidate the same query key.
- `src/store/uiStore.ts` — `connectMode`/`connectSourceId` (6.1's click-to-connect UI state) plus `toggleConnectMode`/`setConnectSource`. Global rather than local `Canvas` state, since `BlockCard` already reads `uiStore` directly for `openPopup` and every block on the canvas needs to react to the mode.
- `src/components/BlockCard.tsx` — a qualifying click (pointer-up under the existing 4px drag threshold) while `connectMode` is on picks a source, then a target, instead of opening the edit popup (double-click-to-open is effectively suspended while connecting, since every such click is now consumed by connect logic); the pending source gets a visible ring. A drag's pointer-up now computes the dragged block's component via `buildComponent` and batch-persists every member's new position at the identical delta, or just its own row if unconnected (6.6).
- `src/components/ConnectionLines.tsx` (new) — one `<svg>` per Area canvas, sized to the blocks' own extent, rendered before the `BlockCard`s so it sits behind them with no `z-index` needed (6.4). Each connection is two overlapping `<line>`s — a thin visible one with an arrowhead marker, and a wide, invisible one on top purely as a bigger click target — whose click opens the confirm dialog before `useDeleteConnection` runs (6.9).
- `src/components/Canvas.tsx` — a "Connect blocks"/"Connecting…" toggle button and a hint line in the toolbar; renders `ConnectionLines` alongside the existing `BlockCard`/`NoteCard` layer; the blocks-or-notes-failed-to-load banner grew a third case for connections.
- No new dependencies. No `Filter`, no content marker, no coupling to Waiting anywhere — picking a Waiting task never creates a connection and a connection never creates a Waiting entry, true by construction (neither code path reads or writes the other's tables), not by an explicit guard.
- **Verified 2026-09-11** in headless Chromium (Playwright, `npm install --no-save`, removed after) driving the real `vite dev` app against a scratch Postgres 16 with `0001`+`0002`+`0003` applied and a stub `auth.uid()` reading `request.jwt.claims`, through a hand-rolled relay translating `/rest/v1/*` into real parameterized SQL under `SET LOCAL ROLE authenticated` with the owner UUID as the JWT claim (genuine RLS, not a superuser bypass) — throwing on any filter operator it doesn't implement and never treating `select`/`order`/`limit`/`offset`/`columns`/`on_conflict` as a filter, both lessons carried forward from earlier phases' own review notes. **30/30 checks passed**, every one asserted against the scratch database directly, not the DOM (full detail under each of `TASKS.md`'s 6.1–6.9): the acyclicity refusal on a real 3-block cycle attempt, with the write confirmed absent rather than merely "no error shown"; the diamond graph the phase brief names specifically, confirming `D` lands in the third column (`A.x + 440`), not the second; a four-block chain at an exact 220px pitch; a drag whose four resulting deltas were compared for numeric equality, not just "all moved"; a merge whose larger side's coordinates were confirmed byte-identical, per block, before and after; a connection delete that moved nothing at all (also byte-identical); and an orphaned block dragging alone once every connection touching it was gone. Two screenshots taken (the diamond graph; the final chain/orphan state). Zero browser console errors across the run.
- **Not checked:** the live Supabase project (this container had no `.env` at session start; a placeholder one was written for the `vite dev` verification run and deleted before finishing, the same pattern every phase since Phase 3 has used); Phase 7's Filter and content marker, which don't exist yet; a rendered line's exact pixel anchor against a card whose height varies with content (the line's `BLOCK_HALF_HEIGHT` is a fixed approximation, same shape as `Canvas.tsx`'s existing `NEW_BLOCK_HALF_HEIGHT` constants, and can be a few pixels off-center against a taller card carrying a Waiting card or wrapped name — cosmetic only, and explicitly out of scope: "no visual design pass"); multi-block *loser* components in a merge whose bridge attaches to a non-boundary (neither source-least nor sink-most) node of their own internal graph — the acceptance criteria and every scenario actually exercised only ever merge a lone block into a larger shape, and `computeConnectionLayout`'s rigid-translation choice for that general case is a documented interpretation (see `TASKS.md`'s 6.5) rather than something this run exercised directly.

### Phase 7 — what was built

The last chunk of the v2 sandbox build. No schema change — Phase 7 needs none; nothing was added to `supabase/migrations/`.

- `src/store/uiStore.ts` — `visibleStatuses: Record<BlockStatus, boolean>` (7.1), all `true` initially, plus `toggleVisibleStatus(status)` flipping exactly the one key touched (no shared "all on" flag to keep in sync with the three individual ones). Transient only, like everything else in this store — not persisted, not scoped to the active Area, matching the spec asking for toggles rather than remembered ones.
- `src/components/Canvas.tsx` — a second toolbar row (7.1) with one labelled checkbox per status; each carries `aria-label="Show <status> blocks"` specifically so it has its own accessible name distinct from `BlockEditPopup`'s own active/upcoming/done status buttons, which render the identical three words elsewhere in the UI. `visibleBlocks = blocks.filter((b) => visibleStatuses[b.status])` is the one line Filter actually is; it's what's mapped into `BlockCard`s and it's what's passed to `ConnectionLines` in place of the full `blocks` array (7.2) — `ConnectionLines` already looks up each connection's endpoints in a map built from whatever block list it's given and silently skips a line whose endpoint is missing, so a connection touching a hidden block disappears without any second filter over the `connections` array itself.
  - **Why this can't accidentally become a layout pass.** `visibleBlocks` is consumed only for *what renders* on this canvas. `BlockCard`'s own drag-and-connect logic (6.6) reads the full, unfiltered `blocks`/`connections` through its own `useBlocks`/`useConnections` calls — a pattern already true before this phase — so a block's membership in a connected component, and therefore what moves when a connected neighbour is dragged, is entirely unaffected by whether that neighbour is currently drawn. Filtering never calls anything that writes `x`/`y`, in any branch.
- `src/components/BlockCard.tsx` — the content marker (7.3): `useLists(block.id)` and `useNotes({ blockId: block.id })`, the same per-block query shape `useWaitingEntries(block.id)` already used here, feed `hasContent = lists.length > 0 || notes.length > 0`. Rendered as a small `bg-slate-800` dot, top-left, `aria-label="Has content"` — top-left specifically because Upcoming's dot (3.4) is top-right, Ready's badge (4.8) is bottom-right, and Focused's outline (5.4) wraps the whole card, so several of these can legitimately appear on one block at once without colliding.
- No new dependencies, no other files touched. `src/lib/lists.ts` and `src/lib/notes.ts` needed no changes — `useLists`/`useNotes` already had exactly the shape `BlockCard` needed, from Phase 3.
- **Verified 2026-09-11** in headless Chromium (Playwright, `npm install --no-save`, removed after) driving the real `vite dev` app against a scratch Postgres 16 with `0001`+`0002`+`0003` applied and a stub `auth.uid()` reading `request.jwt.claims`, through a hand-rolled relay translating `/rest/v1/*` into real parameterized SQL under `SET LOCAL ROLE authenticated` with the owner UUID as the JWT claim (genuine RLS, never a superuser bypass) — throwing on any filter operator it doesn't implement, never treating `select`/`order`/`limit`/`offset`/`columns`/`on_conflict` as a filter, and aliasing every embed subquery `sub` rather than `x` (checked directly against a block's own `x` column in the bridge's own smoke test, since that exact collision is what cost a prior phase a full review cycle) — all lessons carried forward verbatim from this build's own rules. **22/22 checks passed**, every one asserted against the scratch database directly, not the DOM:
  - **7.1** all three toggles default to checked with all 3 seeded blocks (one per status) visible; every combination down to all-off (0 blocks visible) and back to all-on (3 visible) was driven by real clicks and counted in the DOM.
  - **7.2, the check that catches an accidental layout pass:** with a Done block connected to an Active one, toggling Done off hid the Done card and the one connection line touching it (`svg line[stroke="#6b7280"]` count 1 → 0), and all three blocks' `x`/`y` were confirmed **byte-identical**, read back from the database, before and after. Beyond the acceptance line: dragging the still-visible Active block by `(55, 42)` while Done stayed hidden moved the hidden Done block by the **identical** delta too — proving the filter never touches the underlying connection/component data at all, not merely that it happens to leave things alone when nothing's connected.
  - **7.3** a block with only a name showed 0 markers; adding an empty list (confirmed by query: the list exists, 0 tasks in it) brought the marker to 1; deleting that list through its own confirm dialog brought it back to 0; creating only a Note brought it back to 1; a separate block given only a Waiting entry (confirmed by query: 0 lists, 0 notes, 1 real entry) showed the Waiting card but 0 content markers.
  - **The Focus screen / Filter non-coupling named in 5.5** held under direct test: a Focused project was confirmed genuinely hidden by the Active filter on the canvas (0 matching cards), then confirmed still listed on the Focus screen in the same run.
  - Two screenshots taken (`phase7-filter-toggles.png`, `phase7-done-hidden.png`) — the first shows the content marker (black dot, top-left) coexisting with Upcoming's yellow dot (top-right) on the same card, and a Focused block's purple outline alongside a Waiting card's dimmed-blue colour and folded preview, all simultaneously legible; the second shows the same canvas with Done toggled off — the Done card and its connection arrow are simply gone, every other block exactly where it was. Zero browser console errors across the run.
  - **Not checked:** the live Supabase project (no `.env`/credentials at session start, by design — a placeholder one was written for the `vite dev` run and deleted before finishing, the same pattern every phase since Phase 3 has used); a rendered connection line's exact pixel anchor against a variable-height card (pre-existing Phase 6 limitation, untouched here); every possible simultaneous combination of markers beyond the two the screenshots happen to show — the positions were chosen specifically so they can't collide, making this a design property rather than something needing exhaustive combinatorial testing.

## Escalation criteria — Current v2

Always escalate:

* Anything touching the database schema or a migration. Phase 2b specifically: confirm the live backup exists before it runs, not just before the phase starts — this migration rewrites rows that are in real use, v1's did not.
* Anything not explicitly decided in spec or TASKS.md — no guessing at product intent. If something looks like it needs a new open decision beyond the three already resolved, stop and ask rather than picking a default.
* Anything touching auth. Current has its own dedicated Supabase project — no shared-project data boundary to worry about the way Overload's incident did.
* Diagnostic or live SQL queries run through the Supabase SQL Editor bypass RLS entirely — that's a property of the SQL Editor itself, confirmed on Overload, not something specific to Overload's schema. Current has no per-row user_id to filter by (RLS here is pinned directly to auth.uid()), so there's no WHERE-clause equivalent of Overload's rule. Prefer the authenticated client over the SQL Editor for diagnostic reads where that's an option. If the SQL Editor is used anyway and returns more than one account's data, that's still a bug — stop immediately, don't investigate further.
* The builder deleting or overwriting existing data or files unexpectedly during the build — not the app's own confirmed delete features, which are already fully specced.
* A test failure without an obvious, mechanical fix.
* Anything that would change scope, cost, or timeline versus TASKS.md.

Proceed without asking:

* Lint/type fixes, typos, formatting.
* Adding tests for already-specified behavior.
* Following a pattern already established elsewhere in the codebase.
* Anything TASKS.md's acceptance criteria already cover, including the two resolved decisions now patched into the file.

Note, unchanged from v1 and worth keeping verbatim: for schema and auth, "already decided" means the decision doesn't need relitigating — not that the implementation skips a look. Bring the actual diff every time. This is exactly how v1's RLS bug got caught.

## Build log

Running record of chunk hand-offs and escalations. One entry per event, newest last.

Entries before 2026-09-10 predate the v1/v2 split: "TASKS.md" in them means the file now called `TASKS-V1.md`, and their phase numbers are v1's. They are left as written rather than retrofitted.

* **2026-09-08 — Reviewer session started.** Watching the build chunk by chunk; chunks are the TASKS.md phases in order (0 → 5), one phase per builder session, no combining or splitting. On each builder going idle: read its summary, check it against the escalation criteria above, then either start the next phase's builder or hand the decision to Adam and wait.
* **2026-09-08 — Phase 0 builder running** (`session_01VNn4vAkyhpL3K7whuNNj5T`, branch `claude/amazing-hamilton-18epoe`), started outside this session. Cloud sessions aren't reachable as peers for an idle subscription, so the reviewer polls the session record on a self-scheduled check-in instead. Gate is at the end of Phase 0, before Phase 1 starts.
* **2026-09-08 — Phase 0 builder failed; escalated to Adam, build paused.** *(Superseded — the builder resumed after the quota reset and completed. See the entry below.)* The Phase 0 builder went idle at 15:26 UTC having *failed*, not finished: it was cut off by the account-wide five-hour usage limit mid-work (~16 min in, 26.8k output tokens, ~$1.50). **Nothing was pushed** — `claude/amazing-hamilton-18epoe` never reached the remote and its container is disconnected, so what it built is gone and what it touched can't be inspected. Quota reset at 16:50 UTC; a fresh window is open.
  Escalated rather than restarting, on three grounds: (1) "changes scope, cost, or timeline versus what TASKS.md described" is on the always-escalate list, and a phase that consumed budget and produced nothing is exactly that; (2) Phase 1 was never startable — it builds on a scaffold that doesn't exist; (3) Phase 0.3 (auth, RLS, schema) is unavoidably an always-escalate item and the question raised at the reviewer's start is still unanswered.
  Sharpening the decision: **this environment has no Supabase credentials.** A builder cannot create the project, apply the schema, apply RLS policies, or verify 0.3's acceptance criterion. It can only commit SQL to a file and write client code reading `.env`. So 0.3 needs Adam's hands regardless of how the escalation question is resolved.
* **2026-09-08 — Phase 0 completed; reviewed; escalated on RLS.** The builder resumed once quota reset and finished at 17:31 UTC, pushing commit `590399c` to `claude/amazing-hamilton-18epoe`. The previous entry's "failed, nothing survived" read was correct at 17:27 and stale by 17:31.
  **Reviewed against the escalation criteria.** Clean on most counts: schema SQL in `supabase/migrations/0001_init.sql` matches the TASKS.md data model exactly (no extra columns, no ordering, no soft deletes, cascades right); Tailwind defaults only, no config file; Zustand holds transient UI state only; Dexie, vite-plugin-pwa and Recharts correctly not installed; login screen has no sign-up form; `.env` gitignored with `.env.example` committed; no files deleted or renamed; the spec untouched.
  **Escalated — the RLS policy doesn't match the spec.** Every table got `using (auth.uid() is not null)` — *any* authenticated user, not *that* user. Supabase enables email signups by default and the anon key ships publicly in the client bundle, so anyone could sign themselves up and gain full read/write on all four tables. That defeats "the anon key alone must never be sufficient to read or write real data." `verify-rls.mjs` only probes the no-session case, so it would report a false pass. Schema, auth, and an undecided design call all at once — held for Adam rather than picked by the reviewer.
  **Phase 0 is code-complete, not done.** Its acceptance criterion is unverified and unverifiable here: no Supabase credentials in the environment. Adam has to create the project, run the migration, create the one account, and fill in `.env`.
  Phase 1 not started, pending that decision.
* **2026-09-08 — RLS decision made; Phase 0 fix chunk started.** Adam chose: pin the policies to the owner's UID *and* disable email signups in the Supabase dashboard — belt and braces, rather than relying on the dashboard toggle alone, and without adding `user_id` columns the data model doesn't have. Builder started for that fix alone (`session_01RZdhas2NTBdwvRrTe15Prb`, branched from `claude/amazing-hamilton-18epoe`, pushing to `claude/phase0-rls-fix`): UID-pinned policies using an all-zeros sentinel that fails *closed* until Adam substitutes the real UUID, a `verify:rls` check that signup is actually rejected, and honest provisioning steps in CONTEXT.md/TASKS.md.
  Done as its own chunk rather than folded into Phase 1: it's Phase 0 work, it touches auth and the migration, and it deserves a reviewable commit of its own instead of being buried in a Phase 1 diff.
  **Note on branch topology.** The repo's default branch is `claude/affectionate-fermi-3bfmof` (docs only, 9e3b874) — there is no `main`, and Phase 0 is unmerged. Any later builder must be pointed at the newest work branch via `source_revision`, or it will start from docs with no app code. Phase 1 must branch from `claude/phase0-rls-fix`.
* **2026-09-08 — RLS fix reviewed and passed; Phase 1 started.** Fix builder finished in under two minutes (`f775f9a` on `claude/phase0-rls-fix`). Read the diff rather than the summary: all four policies pinned to the all-zeros sentinel with a fail-closed warning block, `verify:rls` now also fails if `signUp()` succeeds, no `user_id` columns added, table DDL untouched, reviewer Build log not present on that branch to disturb. Matches Adam's decision exactly — no *new* escalation, since the schema and auth changes here are the ones he approved — so Phase 1 started without a further hand-off.
  Phase 1 builder: `session_01EbGXUdLNKNHsCsbzqNLmvc`, from `claude/phase0-rls-fix`, pushing to `claude/phase1-primitives`. Popup shell and confirm dialog only; told explicitly not to build delete paths (Phases 2–5), not to touch the database, and not to add a modal library without asking.
  **Three things noted, none blocking.** (1) `verify:rls` treats *any* `signUp()` error as "rejected", so if Supabase refuses the throwaway `@example.com` address for some unrelated reason the script prints that error but still exits clean — a false pass in the convenience check, not in the actual protection, which is the UID-pinned policy. (2) That probe really does create a junk unconfirmed user if signups are open. (3) TASKS.md shows 0.3 as `[x]` although its acceptance criterion is still unverified; the prose directly beneath says so plainly, but the checkbox alone reads as done. Left as-is rather than editing the plan of record unasked.
* **2026-09-08 — Phase 1 built and reviewed; two popup defects found; fix chunk started.** Phase 1 landed as `ef4cce0` on `claude/phase1-primitives`: `Popup.tsx` (portal, Escape, backdrop, focus in), `confirmContext.ts`, `ConfirmDialogProvider.tsx` mounted at the root exposing `useConfirm()` as an awaitable `(message) => Promise<boolean>`. Stayed inside its boundaries — no dependency drift, no database code, no Areas/Canvas/Blocks, Tailwind defaults only, and the confirm API is genuinely shaped so later delete paths must await it.
  **Two real defects in the shared popup, both of which Phase 2 would hit on day one.** (1) Escape is registered per instance on `window`, so with popups stacked one press closes all of them — double-clicking an Area tab opens an edit popup, its Delete opens the confirm on top, and Escaping the confirm would also throw away the edit popup. (2) The backdrop closes on `onClick`, so a press starting inside the dialog and released outside it closes the popup — selecting text in the Area name field and releasing past the edge would discard it.
  Neither is on the always-escalate list — ordinary correctness in a primitive, not a product or design decision — so it was fixed and the build continued rather than handed off. Fix builder: `session_01DK1DwEd5KsCsz4NbPTtwYL`, from `claude/phase1-primitives`, pushing to `claude/phase1-popup-fix`. Phase 2 waits for it, since it is Phase 2 that would trip over both.
* **2026-09-08 — Popup fix verified independently; Phase 2 started.** Fix landed as `b00b0bb` on `claude/phase1-popup-fix`: a module-level open-order stack so only the topmost popup answers Escape, and a backdrop that closes only when mousedown *and* mouseup both land on it. The fix session's summary didn't say what it had actually observed, so the reviewer verified it rather than trusting the report — checked out the branch, ran `npm ci`/`build`/`lint` (all pass), then drove a two-popup harness in headless Chromium. All four cases pass: Escape closes a lone popup; with popups stacked Escape closes only the inner one and leaves the outer open; a backdrop click still closes; a drag starting in the dialog and released on the backdrop does not. Reading a diff is not the same as watching it run — worth repeating for later phases.
  Phase 2 builder: `session_01VmsSNhPdSSw4HHyp3U6hsM`, from `claude/phase1-popup-fix`, pushing to `claude/phase2-areas`. Areas only (2.1–2.4). It is the first phase to query the database from the client, so it was told explicitly that reading and writing the existing tables is expected but any change to the schema or the migration must stop and escalate; that the Area cascade belongs to the database, not to hand-written client deletes; that every delete must await `useConfirm()`; and that with no credentials it cannot verify 2.4's no-orphaned-rows criterion and must not claim otherwise.
* **2026-09-08 — Phase 2 reviewed and passed; Phase 3 started.** Areas landed as `dff2f59` on `claude/phase2-areas`: `AreaTabs`, `AreaCreatePopup`, `AreaEditPopup`, `src/lib/areas.ts` (TanStack Query hooks), `src/types.ts`, and a `Workspace` shell in `App.tsx`. Every hard boundary held — migration, `.env` handling, auth, `supabase.ts` and `package.json` all byte-identical to Phase 1. Delete awaits `useConfirm()` and removes only the `areas` row, leaving the cascade to the database exactly as intended; tabs order by `created_at` with no reorder affordance; server state stays in TanStack Query with only `activeAreaId` in Zustand. Build and lint verified independently in a scratch worktree.
  **Two observations for Adam, neither a spec deviation and neither blocking.** (1) *First run will be silently non-functional until provisioning is finished.* Mutations use `mutateAsync` with no catch, so a rejected write — which is exactly what the sentinel-pinned RLS policy will produce until the real UUID is substituted — closes or stalls the popup with nothing shown. Creating an Area will appear to do nothing at all, with no clue why. (2) *A failed load is indistinguishable from an empty account.* `useAreas` errors fall back to `[]`, which renders "No areas yet — create one above to get started." Neither is a builder error: error handling is not in the spec, and inventing it would have been guessing at product intent, which the escalation criteria forbid. Both are gaps in the spec itself, so they are Adam's to close or to accept as sandbox roughness.
  Phase 3 builder: `session_017gymKQ8zrTjuEzgS2ThBgX`, from `claude/phase2-areas`, pushing to `claude/phase3-canvas`. Canvas and Blocks (3.1–3.5), same hard boundaries, plus explicitly no drag-and-drop library and no Phase 4 block popup.
* **2026-09-08 — Phase 3 built; status-marker defect found by screenshot; fix chunk started.** Canvas and Blocks landed as `bbbdf6c` on `claude/phase3-canvas`: `Canvas.tsx`, `BlockCard.tsx`, `BlockCreatePopup.tsx`, `src/lib/blocks.ts`, `Block`/`Task` types. Boundaries held — migration, `.env`, auth, `supabase.ts`, `package.json` all untouched. Drag is plain pointer events with capture, no library; new blocks centre on the visible canvas; quick-capture splits a textarea by line; build and lint pass.
  **3.4 was broken, and only looking at it caught it.** `upcoming` and `done` both carry Tailwind's `grayscale`, and a CSS filter applies to the whole subtree — so the `bg-yellow-400` marker nested in an `upcoming` card is desaturated with everything else. Rendered and screenshotted: the marker is a faint grey dot and `upcoming` reads as identical to `done` at a glance, which is the exact outcome 3.4 exists to prevent. The builder's summary claimed only that build and lint passed and never said it had looked at the three states; the diff reads perfectly plausibly. Neither reading the code nor trusting the summary would have found this.
  Also fixed in the same chunk: `handlePointerUp` persisted coordinates unconditionally, so every plain click wrote to the database — and Phase 4 adds double-click-to-open to these same cards, which would have meant two pointless writes per open.
  Fix builder: `session_01GSVPrnSMHpSRBFwD22u7iR`, from `claude/phase3-canvas`, pushing to `claude/phase3-marker-fix`. Told to remove the redundant `grayscale` rather than invent a new treatment — a correctness fix to a specified requirement, not a design pass. Phase 4 waits for it, since Phase 4 builds on these cards.
* **2026-09-08 — Marker fix verified by screenshot; Phase 4 started.** Fix landed as `47ab932` on `claude/phase3-marker-fix`: `grayscale` dropped from both dimmed states (the gray background already supplied the dimming, the filter only destroyed the marker), and the position mutation now fires only when the coordinates actually changed. Verified the way the bug was found — rendered all three states and looked at the picture: `active` full blue, `upcoming` and `done` sharing the same dimmed grey body, and the `upcoming` marker now unmistakably yellow. No CSS filter on any card. Build and lint clean; no `.env` committed.
  Phase 4 builder: `session_01AwN9dyLuNqiB28A7NiMnAJ`, from `claude/phase3-marker-fix`, pushing to `claude/phase4-block-popup`. Block popup (4.1–4.5), with particular attention to two things: status must never be inferred from tasks (completing every task must not move a block to Done), and double-click-to-open has to coexist with the existing pointer-event drag without either breaking the other. Told that a passing build proves nothing about what renders, with the marker defect as the worked example.
* **2026-09-08 — Phase 4 reviewed and passed; Phase 5 (final) started.** Block popup landed as `c8f1837` on `claude/phase4-block-popup`: `BlockEditPopup.tsx`, `src/lib/tasks.ts`, `useUpdateBlock`/`useDeleteBlock`, and double-click wiring on `BlockCard`. Boundaries held — migration, `.env`, auth, `supabase.ts` and dependencies all untouched, no `.env` committed, and no ordering column added or queried.
  Status is set only by three explicit buttons; a grep for the usual auto-transition shapes (`every(`, counting completed tasks, assigning `'done'`) finds nothing, so completing every task genuinely does not move a block to Done, as 4.2 requires. Block delete removes only the `blocks` row and leaves tasks to the cascade.
  Drag and double-click were the risky part and the builder handled them well: a 4px distance threshold separates click from drag, and a hand-rolled 400ms double-click timer replaces native `dblclick` because `setPointerCapture` keeps events bound to the element and would otherwise let two quick drags register as a double-click. Verified all of it in a browser — eight checks, all passing: a real drag doesn't open the popup, two quick drags don't either, double-click does, all three statuses render as buttons, no ordering controls exist, task delete and block delete both raise the confirm dialog, and Escaping the confirm leaves the block popup open (the Phase 1 stacking fix doing its job in the situation it was written for). Screenshot confirms the popup renders as specified.
  Phase 5 builder: `session_01WpD51b3cgGT1dJxM1DKP5a`, from `claude/phase4-block-popup`, pushing to `claude/phase5-notes`. Free Notes (5.1–5.5). The brief leans hard on the 5.3/5.4 distinction — auto-discard covers only a note that never held real content, while a note whose content is cleared must survive and require the confirm-gated delete — and forbids adding a "was ever non-empty" column, since that state belongs in the client, not the schema.
* **2026-09-08 — Phase 5 passed. All six phases done; build complete.** Free Notes landed as `43da9c4` on `claude/phase5-notes`: `NoteCard.tsx`, `src/lib/notes.ts`, `Note` type, "+ Note" wired into the canvas. Boundaries held throughout — the migration, `.env` handling, auth, `supabase.ts` and dependencies are byte-identical to Phase 0's, and no "was ever non-empty" column was added.
  The 5.3/5.4 distinction is implemented correctly. `hadContentRef` starts true only if the note already has content, flips true permanently the first time real content is typed, and never flips back; blur discards only when the content is empty *and* that flag is still false. A `deletingRef` set on the Delete button's mousedown stops the textarea's blur from racing the explicit delete — a real hazard, since clicking Delete blurs the textarea first. Drag is confined to a header strip so the textarea keeps normal cursor and selection behaviour.
  Verified against an intercepted PostgREST stand-in so the real mutation paths ran — nine checks, all passing, including the three that matter most: a note that had content and was cleared **survives** blur and is saved as an empty string; a never-had-content note **is** discarded; and the explicit delete raises the confirm dialog and deletes nothing until it is confirmed.
  **All 26 checkboxes in TASKS.md are ticked and the build is complete.** `claude/phase5-notes` is the tip and carries the whole app.
  **Known edge, inherent to the no-new-column constraint:** "was this note ever non-empty" lives only in client state, so it does not survive a page reload. An intentionally-emptied note, after a reload, will be silently discarded if it is focused and blurred without typing. Closing that would need a schema column — Adam's call, and deliberately not taken here.
  **Still outstanding for Adam:** provision Supabase (create project, disable email signups, create the account, replace the sentinel UUID in the migration, run it, fill `.env`, run `npm run verify:rls`) — until then Phase 0.3's acceptance criterion and 2.4's no-orphaned-rows criterion are both unverified, and every write will silently fail; rule on the two Phase 2 error-handling gaps; and consolidate the branches, since there is no `main` and each phase is stacked on the one before.
* **2026-09-09 — Branches consolidated into `main`.** The ten build branches turned out to be a clean linear chain — `claude/phase5-notes` already contained every one of them — so consolidation was one real merge, not eleven: `main` from `claude/phase5-notes`, then the reviewer branch merged in for its Build log. CONTEXT.md auto-merged without conflict, keeping both the per-phase "what was built" sections and this log.
  Sanity-checked rather than re-tested: all 26 TASKS.md items still ticked, all 25 expected source artifacts present, and the four review fixes verified still in place — RLS pinned to the sentinel, no `grayscale` in `BlockCard`, `popupStack` in `Popup`, `hadContentRef` in `NoteCard`, and the signup probe in `verify-rls.mjs`. `npm ci`, `npm run build` and `npm run lint` all clean on the merged tree.
  `main` is not yet the repository's default branch — that is a GitHub setting, and the tools available here cannot change it. The old default, `claude/affectionate-fermi-3bfmof`, still holds docs only.
* **2026-09-09 — Provisioning values applied.** Owner UUID substituted into all eight policy occurrences in `supabase/migrations/0001_init.sql`, and the file's "STOP — READ BEFORE RUNNING" header rewritten, since it instructed the reader to replace a sentinel that is no longer there. `.env` written locally with the project URL and anon key (gitignored, so not committed). The anon key was checked before use: its JWT decodes to `ref: ieszecmiijxkcrxirwuk` and `role: anon`, matching the project. The migration is Adam's to run — see Provisioning above.
  **A caveat on every earlier "build passes" in this log.** Those builds ran without a real `.env`. With the env vars missing, Vite folds `import.meta.env.VITE_SUPABASE_URL` to `undefined`, `src/lib/supabase.ts` throws unconditionally, and the minifier tree-shakes the entire Supabase client out as dead code — 201 kB of bundle instead of 460 kB, with `GoTrueClient` and `RealtimeClient` absent. So those builds proved the TypeScript compiled (`tsc -b` type-checks the real code first) but never proved the app bundles with a real client. The browser behaviour tests are unaffected — `vite dev` neither minifies nor tree-shakes, so real code paths executed there, and Phase 5's checks drove real mutations through an intercepted PostgREST. The build run after `.env` was written is the first to exercise the production path, and it passed.
  **Branch cleanup** could not be done from here — ref deletion returns HTTP 403 through this session's git credentials and the GitHub tools available have no delete-branch operation. Adam removed all eleven in the GitHub UI on 2026-09-09; `main` is now the only branch.
* **2026-09-09 — Phase 0.3 acceptance criterion verified against the live project.** `npm run verify:rls` passes for real: all four tables return no rows with no session, all four writes are refused with PostgREST `42501` ("new row violates row-level security policy for table …"), and signup is refused with `422 signup_disabled`. Those error codes are the point — they could only come from Postgres and GoTrue, which is exactly what the earlier false pass could not show. The tables existing and naming themselves in the RLS errors also confirms the migration applied.
  **Getting there took two fixes, one in the script and one in the environment.** The first live run reported "RLS verification passed" while an egress allowlist blocked the host and not one request left the container — the script counted every error, including a proxy refusal, as a rejection. It is now three-valued: a refusal counts only when it carries a PostgREST error code, or for signup a 4xx from the auth layer; anything else is `UNREACHABLE` and exits 2 saying plainly that nothing was proved.
  **A note for anyone re-running this here:** Node 22 does not read `HTTPS_PROXY` for `fetch`, so `verify:rls` egresses directly and hits a *different* gateway from the one `curl` uses — with a separate allowlist. Adding the host to the environment's egress settings fixed curl but not Node. Run it as `NODE_USE_ENV_PROXY=1 npm run verify:rls` in this container. The npm script is deliberately left plain, since the flag is experimental and only matters behind a proxy.
  **Still unverified: 2.4's "deleting an Area leaves no orphaned rows."** That needs a signed-in session as the owner, and the reviewer has the anon key but not the account password — correctly so. It is a two-minute check for Adam in the running app: make an Area, put a Block with Tasks and a Note in it, delete the Area, then confirm the `blocks`, `tasks` and `notes` tables are empty in the dashboard.
* **2026-09-09 — The two error-handling gaps closed.** Both were spec gaps rather than build defects, and Adam ruled that baseline behaviour here doesn't need his sign-off.
  **Failed writes are no longer silent.** The handling lives on the `MutationCache`, not at call sites, so no mutation can fail quietly — including ones added later that forget to handle it. Failures surface in a red banner above the popup layer, one row per distinct message, dismissible and never auto-hidden, since a failed save is worth noticing late. `src/lib/settled.ts` wraps the nine `mutateAsync` awaits: the cache has already reported the error, so call sites only need to know whether to carry on, and an awaited rejection no longer doubles as an unhandled promise rejection. A popup whose write fails stays open with its input intact.
  **"No areas yet" now means exactly that.** Loading, failed, and genuinely empty are three separate states. A failed load says so, shows the reason, states plainly that nothing has been lost, and offers a retry. `AreaTabs` no longer runs its active-tab fallback unless the load actually succeeded — previously a dropped connection silently cleared the active tab. The same trap existed on the canvas, where failed block or note loads rendered as an empty area, so that surfaces too; strictly an extension of what was asked, and easily reverted if unwanted.
  **Two bugs found by driving it that reading would not have caught.** First, the banner rendered `Couldn't save: [object Object]` — Supabase errors are plain `{code, message, details, hint}` objects, not `Error` instances, so `String(error)` is useless on them. A visible but meaningless message is barely better than a silent failure, which was the entire point. `src/lib/errorMessage.ts` now handles both shapes; the banner reads `new row violates row-level security policy for table "areas" (42501)`. Second, a failed note delete left `deletingRef` stuck true, which makes every later blur skip `commitOnBlur` and silently stop saving that note — a bug only reachable once failures were surfaced at all.
  Verified in a browser against forced 500s and 403s: ten checks, all passing. Note that the failure state appears after TanStack Query's default retries rather than instantly — roughly seven seconds of "Loading areas…" first, which is the right trade for a blip that heals itself.

---

* **2026-09-09 — BUILD COMPLETE AND VERIFIED. This closes the V1 sandbox build.**
  Adam confirmed the app working end to end against the live project — login, and the 2.4 cascade-delete test run by hand. With 0.3 already verified by `verify:rls`, **every acceptance criterion in `TASKS.md` is now met**, not merely coded. All six phases, all 26 items.
  **What review actually caught**, across eleven builder sessions and roughly $17.50:
  * *RLS granted access to any authenticated user, not the owner.* With signups open by default and the anon key public in the client bundle, a stranger could have self-registered into the data. Now pinned to the owner UID, with signups off as the second layer.
  * *Escape closed every stacked popup.* Would have discarded the Area edit popup on every cancelled delete.
  * *The backdrop closed on a drag that merely ended outside it* — select text in a field, release past the edge, lose the input.
  * *The `upcoming` status marker rendered grey*, a parent CSS filter desaturating it, making Upcoming and Done identical at a glance. Found only by screenshotting and looking.
  * Two efficiency fixes: every click on a block was writing to the database, which Phase 4's double-click would have doubled.
  **And two verification bugs — checks that reported success while proving nothing.** These matter more than the defects, because they are what makes a build *look* finished.
  * `verify:rls` counted every error as a rejection, so it printed "RLS verification passed" while an egress allowlist blocked the host and not one request left the container. It is now three-valued: only a PostgREST error code, or a 4xx from the auth layer, counts as a refusal; anything else is `UNREACHABLE` and exits 2 saying plainly that nothing was proved.
  * Every per-phase `npm run build` ran without a real `.env`, which makes Vite fold the env vars to `undefined`, `supabase.ts` throw unconditionally, and the minifier tree-shake the entire Supabase client out as dead code — 201 kB instead of 460 kB. Those builds proved the TypeScript compiled; they never proved the app bundles with a real client.
  **The lesson worth carrying forward:** a clean diff, a green build and a ticked checkbox are all compatible with a broken feature. Three of the four defects were invisible in review and visible in a browser. Build it, run it, and look at it.
  **Error-handling defaults are in place**, closing the two spec gaps: failed writes surface in a dismissible banner naming the real cause, and loading, failed and genuinely empty are three distinct states, so "No areas yet" only ever means zero areas exist.
  Repository state: `main` is the only branch and carries everything; the eleven build branches are deleted. Nothing is outstanding.

---

* **2026-09-10 — V1 closed out as a build; V2 specced and planned. No app code touched.**
  V1 is complete and live and stays that way — what changed is that it is no longer the target. Its plan moved to `TASKS-V1.md` by `git mv`, so its history survives the rename, retitled and marked closed but otherwise untouched. `CURRENT-V2-SANDBOX-SPEC.md` was committed to the repo root and then read back from there rather than from the message it arrived in, so the plan is generated from what is actually committed. `TASKS.md` is now v2's, generated from that spec: seven phases, 40 items, none started.
  **Build order is dependency order, with one deliberate exception.** The expanding-text fix goes first even though it is the smallest item, because it needs no migration — so it lands whatever happens with the schema gate — and because every later phase that renders text reuses the component it builds. Then the schema, then the project canvas (which every remaining phase depends on, since tasks now live in Lists), Waiting, Focused (after Waiting, so the mutual exclusion has something to be exclusive with), Connect, and finally Filter and the content marker, which needs every kind of content to exist before "holding anything" means anything.
  **Two things the spec looks like it needs and doesn't.** There is no `waiting` column — "entries, not one field" makes both Waiting and Ready derived from the entries that exist. And there is no `position` column anywhere: a step-by-step list's order is its creation order, because the spec's task CRUD is add / edit / delete / mark complete and reordering is not among them. Adding either would have been building for a version this plan is deliberately not planning for.
  **The migration was run, not just written.** Against a scratch Postgres 16 seeded with representative v1 data — two Areas, a block with tasks, a block without, tasks in a second Area, notes — the full chain applies clean and every task survives into a Flat list under its original block with its completed state intact, while the block with no tasks correctly gets no list. The new checks accept and reject what they should, and the cascade holds from an Area delete down through lists, tasks, notes at both levels, connections and Waiting entries.
  **Running it changed the plan in two places, which is the point.** The database accepts `A→B` and `B→A` quite happily — a check constraint cannot express reachability — so acyclicity is client-side and the plan says so rather than assuming the schema covers it. And deleting a task that a Waiting entry points at cascades the pick away but leaves the entry standing with nothing in it, which would have read as Ready vacuously; the Ready test now requires every entry to hold at least one pick, and does not depend on the client having tidied the empty entry away, since the task can be deleted from another Area entirely.
  **Three open decisions, each with a working default** so no phase blocks waiting on an answer: what happens to Waiting and Focused when a project leaves Active by a route other than Done; what happens to a picked task deleted by the project that owns it; and whether the task picker reaches across Areas, which Connect restricts explicitly and Waiting does not.
  **The migration risk is genuinely different this time.** V1's ran against an empty database. V2's Phase 2 rewrites rows that are in real daily use, and the scratch-Postgres run proves the SQL, not the data. Back up from the dashboard first.
  Nothing was built. The next step is Phase 1 of `TASKS.md`, or Adam's answers to the three open decisions — in either order, since neither blocks the other.

---

* **2026-09-10 — Two of three open decisions resolved and built into `TASKS.md`; Phase 2 split on the irreversible line. No application code touched.**
  Housekeeping session on the plan, not the app. Adam resolved two of the previous session's three open decisions himself; both are now folded into the phases they govern instead of sitting in `TASKS.md` § Open decisions as defaults.
  **A project can no longer leave Active while genuinely Waiting.** This replaces, rather than refines, the earlier default (Focused cleared on any exit, Waiting entries kept but hidden). The new rule is a hard gate: any non-Ready Waiting entry blocks a status change off Active until the project reaches Ready, per 4.4's existing definition — landed as Phase 4's new 4.5, with its own acceptance criterion, pushing the rest of Phase 4 (4.5–4.9) down to 4.6–4.10.
  **Deleting a task that something is Waiting on now warns before it happens.** The pick disappearing with the task, and an emptied entry going with its last pick, was already the previous session's default; what's new is that the confirm dialog now checks for this first and names the waiting project in its warning, so the loss is disclosed rather than merely tidied up silently afterward. No second confirmation beyond that — the warning stands in for one. Landed as Phase 3's new 3.5, right after 3.4's existing task-delete confirmation, pushing 3.5–3.7 down to 3.6–3.8; cross-referenced from the new Phase 4.9 (entry delete), since the two are easy to confuse and are deliberately handled at different points — a picked task's own delete lives in 3.5, a Waiting entry's own delete is 4.9, and neither raises the other's confirmation.
  **The third decision — whether the Waiting task-picker reaches across Areas — stays open**, exactly as it was, now the sole entry in § Open decisions, renumbered from 3 to 1 since it's alone.
  **Phase 2 is split into 2a and 2b, on the one line that actually matters: reversible versus not.** 2a is everything that can be redone if it's wrong — writing `0002_v2.sql`, RLS on the four new tables, and the client compatibility layer — proven with a *fresh* dry run against a scratch database at build time, deliberately not by citing this planning session's earlier run, since the migration file can drift between planning and building. 2b is the one line that can't be redone — running that same file against the live project — and does nothing until a live backup is confirmed to exist immediately beforehand, not merely at some point since the phase began. Every other reference to "Phase 2" in `TASKS.md` and here was checked and updated to say 2a or 2b specifically, or generalized where either could apply.
  **`CONTEXT.md`'s escalation criteria were replaced, not appended to.** The old `## Escalation criteria` section (itself already updated for v2 last session) and the new `## Escalation criteria — Current v2` Adam supplied this session covered nearly the same ground — schema/migration, undecided product intent, auth/data boundaries, unexpected deletes, test failures, scope creep — so keeping both would have left two overlapping, partly stale lists answering the same question. The old section is gone; the new one, added verbatim as given, is now the only one. It adds three things the old one didn't have: a named gate on Phase 2b specifically (confirm the backup right before running, not just at the phase's start); an instruction to stop and ask rather than invent a fourth open decision if the build turns one up; and a rule that any live/diagnostic SQL outside the migration itself must filter to the owner's user_id, with an unconditional stop if another user's data ever surfaces.
  **Two things in that new section as given don't quite match what's actually in this repo, flagged rather than silently fixed:** it was added exactly as supplied, per instruction, not edited to resolve either. First, the user_id-filtering rule presumes a `user_id` column; this schema has none on any table — Phase 0's RLS policies compare `auth.uid()` directly, by design (see Phase 0's build notes above). Second, it refers to "the shared Supabase project," but `## Database` above has said from the start that this is "a fresh, dedicated Supabase project. No shared backend or cross-app data access with Overload." Both are worth Adam's eyes; neither was resolved unilaterally here.

---

* **2026-09-10 — Both flagged lines in the escalation criteria corrected in place. No application code touched.**
  Housekeeping only, on `## Escalation criteria — Current v2`. The two mismatches the previous entry flagged rather than fixed — added verbatim then, since that session was told to add the section exactly as given — are now corrected in place, on Adam's own replacement text.
  **The SQL Editor line is rewritten, not just re-scoped.** It no longer describes itself as a `user_id` filtering rule Current can't satisfy; it says what actually applies here: the Supabase SQL Editor bypasses RLS entirely, a property of the tool itself rather than of any one schema, confirmed on Overload. Current has no `user_id` column to filter by — RLS is pinned directly to `auth.uid()` — so there's no WHERE-clause equivalent of Overload's rule to carry over; the authenticated client is preferred for diagnostic reads where that's an option, and the SQL Editor returning more than one account's data is still a stop-immediately bug regardless.
  **The data-boundary line is narrowed to what's actually true here.** It no longer references "the shared Supabase project" — Current doesn't have one; `## Database` above has said so since before this section existed. The line now just escalates anything touching auth, with a one-line note on why the shared-project concern from Overload's incident doesn't carry over.
  Every other line in the section, and the section's structure, is untouched from the previous session's version.

---

* **2026-09-10 — V2 reviewer session started; chunk 1 builder found and being watched. No application code touched.**
  Reviewer for the v2 build, same arrangement as v1: chunks are `TASKS.md`'s phases in order, one phase per builder session, no combining or splitting. Phase 2's split means eight chunks, not seven — **1, 2a, 2b, 3, 4, 5, 6, 7**. On each builder going idle: read its summary, check it against `## Escalation criteria — Current v2`, then either start the next chunk's builder or hand the decision to Adam and wait for a reply.
  **The v2 plan was not on `main`, and this session started without it.** `main` is still v1 as closed on 2026-09-09; `TASKS.md`, `CURRENT-V2-SANDBOX-SPEC.md`, `TASKS-V1.md` and the v2 escalation criteria all lived only on `claude/optimistic-davinci-hrqlw1`, six commits ahead and unmerged. Read from `main` alone, `TASKS.md` is v1's with all 26 items ticked and no v2 chunks in it at all — so the plan of record has to be fetched deliberately, not assumed to be on the default branch. This reviewer branch (`claude/brave-gates-n66ll3`) was fast-forwarded onto that planning branch, which was clean (`main` is its ancestor), so the build log below appends to the v2 file rather than to a stale v1 copy. This is the same trap v1's "Note on branch topology" recorded on 2026-09-08, in a new form: then the default branch held docs and no app code, now it holds app code and no plan.
  **Every builder this session starts will be given an explicit `source_revision`** rather than inheriting the default branch, for exactly that reason.
  **Chunk 1 builder: `session_01DdDHWuoLXADbGKyDgY2Lft`** ("Expanding text input component Phase 1", branch `claude/expanding-text-input-phase-1-ojftae`), started by Adam outside this session at 20:23 UTC, running. Its `sources` carry no `revision`, so it started from the default branch without the plan — and its own first action was to fast-forward to the V2 planning commits, so it found the gap itself. Noted rather than corrected: it is already on the right footing.
  Cloud sessions are not reachable as peers for an idle subscription — `ListAgents` sees none, unchanged from v1 — so the watch is a self-scheduled check-in polling the session record, exactly as v1's reviewer did.
  **Nothing on the always-escalate list is triggered yet**, and no chunk has been started by this session. Two things are being carried forward rather than raised now: `TASKS.md` § Open decisions 1 (whether the Waiting task-picker reaches across Areas) is unanswered but has a working default and is not needed before Phase 4; and **Phase 2b's backup gate is Adam's to confirm and will not be assumed** — 2b does not start on this reviewer's say-so, and a confirmation has to be immediately before the run, not merely at some point since the phase began.

---

* **2026-09-10 — Open decision 1 answered by Adam: the Waiting task-picker does *not* reach across Areas. Folded into `TASKS.md`; no open decisions remain. No application code touched.**
  Adam's call, given while the chunk 1 builder was still running, so nothing was built against the old default and nothing needs unwinding. **Waiting now matches Connect**: the picker offers tasks from other projects in the same Area only — never the project's own, never another Area's. The spec's silence on the point (Connect restricts explicitly at spec line 37, Waiting says only "other projects" at line 55) was an omission, not a deliberate contrast.
  Folded into the phase it governs rather than left in § Open decisions, the same treatment the other two decisions got: **4.3** now states the rule outright, with the note that a pick and its task therefore always share an Area, so the picker's candidate query is scoped by the Area the canvas has already loaded and no cross-Area read is needed to render a Waiting card. **§ Open decisions is now empty** — all three resolved, none defaulted.
  **One consequence needed handling rather than a find-and-replace, and it is the reason this was not a one-line edit.** 4.4's Ready test requires every entry to hold at least one pick, and justified that with "the task can be deleted from another Area entirely" — a reason this decision has just made false. **The rule stays; the reason is replaced.** The tidy-up in 3.5 is client-side and so not guaranteed: the delete can fail (v1 built the error banner precisely because writes do fail), the client can be stale, and a row can be removed outside the app through the Supabase dashboard. Ready is derived on read, so it must be correct against whatever the database actually holds, not against an assumption that the client tidied up first. Dropping the clause along with its stale reason would have reintroduced the vacuous-Ready bug the scratch-Postgres run found — which is exactly the sort of thing a mechanical edit loses.
  **3.5 was deliberately left alone.** It checks whether *any* Waiting entry on *any* project holds the task being deleted. That is now a superset of what 4.3 can produce, and it stays that way on purpose: the check costs nothing, and narrowing it to the current Area would mean a pick that exists outside the expected scope stops raising its warning instead of surfacing as a bug.
  **One edit was made to Adam's own escalation-criteria text and is flagged here rather than made silently:** the line reading "a third open decision beyond the two already resolved" now reads "a new open decision beyond the three already resolved". Arithmetic consequent to this decision, not a change to the rule, which is unchanged — don't settle a new decision with a default, stop and ask. Easily reverted if the original wording is preferred. The earlier build-log entries that describe the decision as open, and the 2026-09-10 entry carrying the same now-stale "another Area" reasoning, are **left as written** — the log records what was true when written and is not retrofitted, per the convention stated at the top of it.

---

* **2026-09-10 — Phase 1 (expanding text inputs) built and verified.** First application code on the v2 plan. Landed one component, `AutoGrowTextarea`, and wired it into the two places the spec names: `NoteCard`'s content box and, in `BlockEditPopup`, both `TaskRow`'s inline edit field and the "Add a task" field — no schema change, no new dependency, nothing else touched (Block/Area names and the unrelated quick-capture bulk-entry field in `BlockCreatePopup` are exactly as v1 left them).
  **The escalation-relevant judgment call:** converting task text fields from `<input>` to `<textarea>` changes what Enter does in them — it now inserts a newline instead of committing (`TaskRow`) or submitting (Add-a-task). This isn't a side effect to route around; multi-paragraph task text is the acceptance criterion's own example, so a single-line input's Enter-commits behavior has to go for that field to be usable at all. `TaskRow` still commits on blur; "Add a task" now submits only via its button. Nothing in `TASKS.md` or the escalation criteria calls this out as needing a decision, and it doesn't touch schema, auth, or an undecided product question, so it was made and documented here rather than escalated.
  **Verified in a browser, not just by reading the diff** — the project's established bar, and the one that caught v1's real defects. Same pattern as every v1 phase: a temporary in-memory mock over `src/lib/supabase.ts`, restored before committing; Playwright installed with `npm install --no-save` for the session's Chromium runs and removed afterward, since it isn't and shouldn't become a project dependency. Typed three long paragraphs into a Note and into a task's inline field: both grew to fit with `scrollHeight` matching `clientHeight` (no inner scrollbar) and every line visible in a screenshot; clearing the content returned both to their original single-line height. The Add-a-task field grows the same way, and a newline typed mid-entry survived until the Add button was clicked, after which it emptied and shrank back. `npm run build` and `npm run lint` both pass clean.
  `TASKS.md` 1.1-1.3 marked done with the verification note above. Phase 2a (schema and migration, proven against a scratch database, not yet the live project) is next.

---

* **2026-09-10 — Phase 1 reviewed. One defect found by measuring, one product question escalated to Adam. Phase 2a NOT started; waiting on his answer.**
  Phase 1 landed as `c12cbee` + `f5c294d` on `claude/expanding-text-input-phase-1-ojftae` (base `fa758d1`): `AutoGrowTextarea.tsx`, wired into `NoteCard`'s content box and both task text fields in `BlockEditPopup`. **Every hard boundary held** — five files touched, none of them the migration, `package.json`, `supabase.ts`, `.env` or anything auth-related; no dependency added (Playwright was installed `--no-save` and is absent from the committed tree); `blurOnEnter` is not dead code, it still serves the Block name field. The builder's own write-up is unusually honest: it named the Enter behaviour change itself rather than burying it.
  **Verified independently rather than on the summary**, per the v1 lesson. Ran it: `npm ci`, `npm run build` (462 kB — the real-client path, not the 201 kB tree-shaken build that made every v1 "build passes" hollow), `npm run lint` clean, then drove it in Chromium against an intercepted PostgREST so the real hooks and mutation paths executed. **1.2 genuinely works** — a note holding three long paragraphs grows 28px → 268px with `scrollHeight === clientHeight`, every line visible in a screenshot, and clearing it returns it to exactly 28px.
  **Defect found, and only measuring found it: `AutoGrowTextarea` is short by its own border width.** It writes `el.style.height = el.scrollHeight + 'px'`, but Tailwind's preflight sets `box-sizing: border-box`, so that height is the *border* box while `scrollHeight` describes the content box. On the bordered task fields: `style.height 104px`, `offsetHeight 104`, `clientHeight 102`, `scrollHeight 104` — `scrollTopMax` of exactly 2, the two 1px borders. The box is silently 2px scrollable with `overflow: hidden` covering it. **The note field has no border, which is precisely why it measures clean and the bug hid there** — and why the builder's claim that "`scrollHeight` matched `clientHeight` in both" is true of the note and false of the task fields. Today it costs 2px of bottom padding rather than text, because the 4px `py-1` absorbs it; the reason it still matters is that Phase 3 reuses this component for project-canvas notes and list task fields, and any bordered instance with less than 2px of bottom padding will clip real text, against 1.1's "nothing truncated". Mechanical fix — add `offsetHeight - clientHeight` back. **Ordinary correctness in a shared primitive, not a product call**, so it is the reviewer's to fix, exactly as v1's two popup defects were.
  **Escalated — Enter no longer adds a task.** Confirmed by driving it, not inferred: typing in "Add a task" and pressing Enter leaves the task count at 1 and the field holding `"first line\n"`. In v1 that keystroke appended the task, and it was verified doing so. The `<input>` → `<textarea>` swap removes the implicit form submit. For `TaskRow`'s inline edit field this is forced — 1.3's own acceptance criterion wants multi-paragraph task text, so Enter has to make a newline there. **For the add field it is a genuine choice**, and both options are coherent: auto-grow works just as well with Enter still submitting, since a long single-line task wraps and grows anyway, and newlines can be added afterward by editing inline. That makes it a new open decision about product intent on the daily fast path for capturing tasks — which the escalation criteria say to stop and ask about rather than settle with a default. The builder considered it and decided it did not qualify; this reviewer disagrees, narrowly and only for the add field.
  **One observation, not a defect and not blocking:** an empty note is now a single-line box rather than v1's `h-24`, so a freshly created note (5.1 creates it empty and focused) is a much shorter card than before. That is what "height follows content, no fixed rows" asks for and it satisfies 1.1's acceptance, so it is recorded rather than raised — worth a word from Adam only if daily use says it wants a minimum height.
  **Phase 2a not started.** Both findings land in the same two files, so they will be one fix chunk once Adam answers, rather than two builder sessions touching `BlockEditPopup` in sequence. Phase 1 stays on its own branch, unmerged, until that fix lands.

---

* **2026-09-11 — Adam answered the Enter question; Phase 1 fix chunk started.** Decision: in the **"Add a task" field only, Enter adds the task and Shift+Enter inserts a newline.** This restores v1's keyboard fast path — type, Enter, type, Enter — without giving up multi-line entry up front, and the field still auto-grows as long text wraps. `TaskRow`'s inline edit field is deliberately untouched: Enter there correctly makes a newline, since 1.3's acceptance criterion wants multi-paragraph task text and that field commits on blur rather than on Enter. The Block name field keeps its `blurOnEnter`.
  **Both findings go into one chunk**, not two, since they land in the same two files and a second builder session touching `BlockEditPopup` in sequence would cost a review round for nothing. Fix builder: `session_01H8Tzz15mSS19Rwic8Kkpkh`, from `claude/expanding-text-input-phase-1-ojftae` **with an explicit `source_revision`**, pushing to `claude/phase1-autogrow-fix`.
  The brief carries the border-box diagnosis in full — that `style.height` sets the border box while `scrollHeight` describes the content box, with the measured numbers — so the builder fixes the cause rather than rediscovering the symptom, and is told explicitly not to switch the component to `content-box` and fight the project's Tailwind defaults. It is also told to leave `TaskRow` and the Block name field alone, and to report real numbers rather than the word "verified": `scrollTopMax` must be 0 on a bordered field, the task count must actually rise on Enter, and Shift+Enter must leave a real `\n` without submitting.
  **Three container gotchas were handed over rather than left to be rediscovered**, having cost this reviewer time during the Phase 1 review: Playwright from npm expects a browser build that isn't present, so it needs `executablePath: '/opt/pw-browsers/chromium-1194/chrome-linux/chrome'` instead of `playwright install`; a fresh container has no `.env`, and without one Vite folds the env vars to `undefined` and the minifier tree-shakes the entire Supabase client out, giving a ~201 kB bundle that proves far less than it appears to (a real build is ~460 kB); and intercepting `fetch` for `/rest/v1/*` with a seeded `localStorage` session is a more faithful harness than mocking over `src/lib/supabase.ts`, because the real hooks and mutation paths then execute.
  Phase 1 stays unmerged on its own branch until this fix lands and is reviewed. Phase 2a still not started.

---

* **2026-09-11 — Phase 1 fix chunk: two defects closed on `claude/phase1-autogrow-fix`, branched from `claude/expanding-text-input-phase-1-ojftae`.** Not new scope — two corrections to what Phase 1 landed, caught by measuring rather than reading, exactly this project's established lesson.
  **Fix 1 — `AutoGrowTextarea` was short by its own border width.** `style.height = scrollHeight` looks right until Tailwind's preflight `box-sizing: border-box` is accounted for: `style.height` sets the border box, `scrollHeight` describes the content box, and the gap between them is the border width. Measured on a bordered task field in `BlockEditPopup` **before** the fix: `clientHeight 438`, `scrollHeight 440`, `offsetHeight 440`, forcing `scrollTop` to a large value and reading it back gave `2` — the two 1px borders, hidden by `overflow: hidden`. **After** the fix: `clientHeight === scrollHeight` (368/368 in the actual verification run below), forced `scrollTop` reads back `0`. Fixed by computing `el.offsetHeight - el.clientHeight` right after the `height: 'auto'` reset and adding it to `scrollHeight` before writing the final height — a one-line change, comment updated to say why the term is there. The borderless Note field is unaffected: the added term is 0 there, so its behaviour is unchanged.
  **Fix 2 — Enter now adds the task again in "Add a task."** Adam's decision (asked and answered, not guessed): in that field only, Enter submits the form and Shift+Enter inserts a newline, restoring v1's verified Enter-to-add behaviour that Phase 1's `<input>`→`<textarea>` swap had silently dropped. `onKeyDown` calls `event.currentTarget.form?.requestSubmit()` on a plain Enter and does nothing on Shift+Enter. The Add button's own path, `TaskRow`'s inline edit field (Enter still inserts a newline, still commits on blur), and the Block name field (`<input>`, Enter-blurs) are all untouched, per the fix chunk's explicit boundaries.
  **Verified in Chromium** (Playwright installed with `npm install --no-save`, never committed; a placeholder `.env` pointing `VITE_SUPABASE_URL` at the real project's host — `https://ieszecmiijxkcrxirwuk.supabase.co`, not a secret, only the ref already named throughout this file — with a dummy anon key, so the production build actually bundles the Supabase client at ~462 kB instead of tree-shaking it away; `page.route` intercepted `**/rest/v1/**` and `**/auth/v1/**` with an in-memory PostgREST stand-in, and a mock session was seeded into `localStorage` under `sb-ieszecmiijxkcrxirwuk-auth-token` before load, so the real hooks and mutation paths in `src/lib/*.ts` executed against that stand-in rather than a mocked-over `supabase.ts`). 13 checks, all passing: signed in from the seeded session with no login screen; created a real Area and Block through the UI; in "Add a task," Enter increased the task count (0 → 1) and cleared/shrank the field; Shift+Enter left the literal string `"Line A\nLine B"` in the field without changing the task count, and a following plain Enter submitted it, with the stored row's `text` field confirmed (via the mock store) to contain a real `\n`; typing three long wrapping paragraphs into that same bordered field gave `clientHeight 368`, `scrollHeight 368`, forced-`scrollTop` `0`; the Note field grew from 28px empty to 408px with three paragraphs and back to 28px cleared, matching the range from Phase 1's own verification. Screenshotted the block popup with two tasks (one plain, one multi-line) and the grown bordered field — every paragraph fully visible, no clipping, no inner scrollbar. Sanity-checked the fix itself by re-running the same harness against the pre-fix component: it reproduced `clientHeight 438` / `scrollHeight 440` / forced-`scrollTop` `2` exactly as CONTEXT.md's fix-request described, then passed again once the fix was restored.
  **What was not checked:** anything against the live Supabase project — no credentials in this environment, per the standing rule; the note's 5.3/5.4 auto-discard-vs-survive behaviour (untouched by this chunk, not re-verified); cross-browser behaviour outside Chromium; the Block-name field and any other single-line `<input>` (explicitly out of scope, confirmed only by reading, not driving). `npm run build` (with the placeholder `.env` in place so Vite doesn't fold the env vars to `undefined`, ~462 kB bundle) and `npm run lint` both pass clean. `package.json`, `package-lock.json`, `supabase.ts`, and auth are byte-identical to before this chunk; no schema change, no migration, no new dependency.
  `TASKS.md` 1.3's acceptance note extended with this fix chunk. Phase 2a is still next; nothing else in the plan was touched.

---

* **2026-09-11 — Phase 1 fix reviewed and passed; Phase 1 merged; Phase 2a started.**
  Fix landed as `01279e7` on `claude/phase1-autogrow-fix`. **Verified independently rather than on the report** — the fix builder claimed 13 passing checks, and the Phase 1 builder had also claimed a clean verification while shipping the border-box defect, so the report was re-run rather than believed. 13 checks of this reviewer's own, in Chromium against an intercepted PostgREST with a seeded session, all passing.
  **The two numbers that decide it.** A bordered task field holding several paragraphs now reports `clientHeight 224`, `scrollHeight 224`, `offsetHeight 226`, `scrollTopMax 0` — the 2px gap between the border box and the content box is exactly what `offsetHeight - clientHeight` puts back, and the hidden scroll is gone. Enter in "Add a task" raises the task count 1 → 2, clears the field and shrinks it from 56px back to its original 32px; Shift+Enter leaves `"first line\n"` in the field and changes the count not at all; the stored row keeps a real `\n`; Enter on an empty field creates nothing (`handleAddTask` trims first). `TaskRow`'s Enter still inserts a newline and still does not submit — untouched, as instructed. The borderless Note field is unchanged: 28px empty → 248px with three paragraphs → 28px cleared, `scrollTopMax 0` throughout. Screenshotted and looked at: padding is even top and bottom now, and the descenders on the last line sit clear of the edge.
  **Boundaries held**: the migration, `package.json`, `package-lock.json`, `supabase.ts`, and every `src/lib` hook are byte-identical to the v2 plan base; no `.env`, Playwright or harness in the committed tree.
  **Merged** Phase 1 and its fix into `claude/brave-gates-n66ll3` and `main` as `dbbc0de`. The one merge conflict was both sides appending to this build log; resolved by keeping every entry and interleaving them in the order they were actually written, rather than dropping either side. `TASKS.md` 1.3's original acceptance note said Enter inserts a newline instead of submitting — true when written, false now — so that sentence is marked superseded rather than left to contradict the fix note directly beneath it. The merged tree builds at 462 kB (the real-client path) and lints clean.
  **Phase 2a builder: `session_01Npnnu1vUWWN6Ur3xK3gs8p`**, from `claude/brave-gates-n66ll3` with an explicit `source_revision`, pushing to `claude/phase2a-migration`.
  **Why 2a was started rather than handed to Adam, even though schema is on the always-escalate list.** The list's own closing note settles it: for schema, "already decided" means the decision doesn't need relitigating, not that the implementation skips a look — bring the actual diff every time. Phase 2 was split precisely so the reversible half could proceed: 2a writes the migration and proves it against a throwaway database and cannot touch the live project, while 2b — the one irreversible step — is gated. So 2a proceeds and its diff gets a hard look; **2b does not start on this reviewer's say-so under any circumstances.**
  **One practical problem was foreseen and handed over rather than left to be discovered or faked.** 2a.2's acceptance asks for `npm run verify:rls` to pass against the scratch database, but the script talks to PostgREST and GoTrue and this container has Postgres 16 and `psql` and no Supabase CLI or PostgREST at all — so the script cannot run end to end there. The builder is told to prove the policies directly in SQL instead, per table and per operation, and to state plainly that the script itself is verified only by reading until 2b runs it for real. It is also pointed at v1's false pass, recorded in this file, as the reason a check that reports success while proving nothing is worse than no check.

---

* **2026-09-11 — Reviewer pre-staged a scratch-Postgres harness while Phase 2a builds. No application code touched.**
  Preparation, not review — 2a is still running. Recorded because the container is ephemeral and the method is worth more than the scripts.
  **A local stand-in for the parts of Supabase the migrations depend on.** Postgres 16.13 initialised as its own throwaway cluster on port 5433, plus a stub supplying what `0001_init.sql` and `0002_v2.sql` actually reference: a `create schema auth`, the `anon` and `authenticated` roles, and an `auth.uid()` that reads `request.jwt.claims` exactly as Supabase's does. On that, `0001_init.sql` applies clean and a representative v1 seed loads — two Areas, a block with three tasks, a block with none, a task in the second Area, two Area-level notes, two tasks already `completed`. That is the fixture 2a.1's fresh dry run needs, ready before the migration arrives.
  **Two container facts worth keeping.** `initdb` refuses to run as root and the session scratchpad is mode 700 root, so the postgres user cannot traverse it — the data directory has to live somewhere postgres owns (`/var/lib/postgresql`), even though the scripts can stay in the scratchpad. And `psql` connecting as `postgres` is a **superuser, which bypasses RLS entirely** — every RLS probe has to `set local role anon` or it proves nothing at all. That is the same trap, in local form, that this file already records for the Supabase SQL Editor.
  **The RLS probe is stronger than `verify:rls`, in the one way that matters.** It tests each table under three identities rather than one: no session at all, a *stranger* with a different uid in the JWT claims, and the owner. Against the four v1 tables it reports, for every table, `0` rows and a write refused with `42501` for both no-session and stranger, and the real row counts for the owner (areas 2, blocks 3, tasks 4, notes 2). **The stranger case is the one v1's script never had** — `verify:rls` only ever probed "no session", which is precisely why Phase 0's `using (auth.uid() is not null)` bug passed it and had to be caught by reading the policy instead. Proving the owner direction matters too: a UUID matching no real user denies everyone, which fails closed but would also mean a dead app, and only the owner probe can tell the two apart.
  The scripts are deliberately **not committed**. They are reviewer tooling, `TASKS.md` does not ask for them, and adding them to the repo would be a scope change however small. The method above is the durable part; 2a's builder is writing its own proof and will be reviewed on that.

---

* **2026-09-11 — Phase 2a built and proven against a scratch database. The live project is untouched.** Builder for `claude/phase2a-migration`, branched from `claude/brave-gates-n66ll3` (Phase 1 built, reviewed and merged). Scope held to 2a.1–2a.3 exactly, per the brief: no `position` or `waiting` column added, `0001_init.sql` untouched, no live credentials sought or found, nothing run against the live project.
  **2a.1 — `supabase/migrations/0002_v2.sql`**, exactly the Data model delta from `TASKS.md`, in order, then RLS on the four new tables appended in the same file (2a.2, same file as `0001_init.sql`'s own schema+RLS combination). Ran Postgres 16 locally (`/usr/lib/postgresql/16`, an existing but stopped cluster — started it), created a scratch database, and — since the postgres OS user couldn't read files under this session's locked-down scratchpad directory — created a `root` Postgres role so peer auth let the session's own OS user (`root`) connect directly without copying files anywhere else. Seeded representative v1 data (two Areas; a block with three tasks including one completed; a block with zero tasks; a second Area with a block holding two tasks including one completed; two Area-level notes) and ran the committed migration file fresh. Result: all 5 tasks survived, each still under its original block via its new list's `block_id`, each in a `kind='flat'` list, text and completed state unchanged; the zero-task block got zero lists; `tasks.block_id` is gone. Went further than the acceptance line to exercise what `TASKS.md`'s Data model section already claimed: `notes_one_parent` accepts existing area-only rows and a new block-only row, rejects both-null and both-set; `lists.kind`/`waiting_entries.kind` reject bogus values; `connections` rejects a self-edge and a duplicate edge but accepts both directions of a 2-block cycle, exactly as documented (client-side enforcement is Phase 6's job); and the full cascade from an `areas` delete was checked with real row counts before and after — blocks, lists, tasks, notes at both levels, connections, and a waiting entry with a picked task all gone, a sibling Area untouched.
  **2a.2 — RLS on `lists`, `connections`, `waiting_entries`, `waiting_entry_tasks`,** pinned to the same owner UUID copied from `0001_init.sql`'s own eight occurrences. `scripts/verify-rls.mjs` extended to all eight tables (`writeProbes` gained four entries; the rest of the script already loops generically). **Honestly short of a real run, and said so rather than claiming otherwise.** This container has no Supabase CLI, no PostgREST, no GoTrue — checked, not assumed: `dockerd` does run here (the `service docker start` wrapper fails on a `ulimit` permission error, but running `dockerd` directly works), but `docker pull postgrest/postgrest` was blocked by the environment's egress allowlist (`Forbidden` fetching the image blob from Docker Hub's CDN) after the manifest came through — so PostgREST genuinely isn't reachable here, not merely un-attempted. Fell back to the brief's expected route: built a minimal `auth.uid()` stub function and `anon`/`authenticated` roles directly in the scratch database (mirroring what a real Supabase project's bootstrap provides), granted `anon` the same DML Supabase grants by default, and probed all eight tables as `anon` under two identities inside rolled-back transactions — no `request.jwt.claims` set at all, and a `claims` value naming a different uid. Every table, both identities: `select count(*)` is 0; `insert` fails with Postgres SQLSTATE `42501` (confirmed literally via a `DO` block catching the exception — that SQLSTATE is exactly what PostgREST would surface as its `code` field, which is what `verify-rls.mjs` itself checks for); `update`/`delete` affect 0 rows, since `using()` leaves nothing visible for them to touch (the same reason the script's own write probes only ever test inserts). `verify:rls`'s real run against PostgREST and GoTrue is still owed, at 2b, against the live project.
  **2a.3 — client changes**: `src/types.ts` grown to mirror the full `0002_v2.sql` delta; new `src/lib/lists.ts` (`useLists`, `useCreateList`, deliberately no update/delete — no list CRUD UI exists yet); `src/lib/tasks.ts` rekeyed from `blockId` to `listId`; `src/lib/notes.ts` reshaped around a `NoteParent` union (`{areaId}` | `{blockId}`) so the same hooks already work for Phase 3's block-level notes, though nothing calls them that way yet — `Canvas.tsx` still only ever passes `{ areaId }`; `src/lib/blocks.ts`'s `useCreateBlock` now creates a Flat list before its quick-capture tasks when there are any; `BlockEditPopup.tsx` reads `lists[0]` as its block's one list and lazily creates it (same fixed `(24, 24)` origin as the migration and quick-capture) the first time a task is added to a previously listless block. No project canvas, list CRUD, kind picker, Waiting, or Connect — the running app looks and behaves exactly as it did under v1's shape.
  **Verified in a browser against the actual migrated shape, not a hand-written fixture** — the brief's preferred route, attempted and it worked. Real PostgREST wasn't available (see 2a.2 above), so a small hand-rolled stand-in was built instead: a Playwright `page.route` handler on `/rest/v1/**` that parses supabase-js's PostgREST-shaped requests (`eq.` filters, single-column `order`, `Prefer`-driven insert return shape) and executes them as real parameterized SQL against the same scratch Postgres 2a.1 was proven against, via `pg` (installed `--no-save`, removed after). `/auth/v1/**` stubbed and a session seeded into `localStorage`, same pattern as the Phase 1 fix chunk. Drove the real `vite dev` app in Chromium (Playwright, `--no-save`, removed after; a placeholder `.env` so the build bundles the real Supabase client) through create/rename/delete Area, create-Block-via-quick-capture and create-Block-with-none, the block popup's full task CRUD (add/toggle/edit/delete, all confirmed to land in or leave the one list — no second list ever created), the lazy list-creation path on a listless block, Note create/type/blur, and Area delete's cascade — 20 checks, every one asserted against the scratch database directly via a second, independent `pg` connection, not against the DOM. Two screenshots taken (block popup mid-edit; canvas with a note card). One false failure came from the test itself, not the app, and is worth recording: two quick-captured tasks inserted in a single statement share one `created_at` (transaction-time `now()`), so `order by created_at` has a real, unbroken tie between them — which is exactly the "flat, unordered list, no ordering column" behaviour `src/lib/tasks.ts`'s own comment already describes, not a bug. The check was rewritten to key off the task a checkbox's `aria-label` actually names instead of re-deriving order from a tied sort.
  `npx tsc -b`, `npm run build` (with a placeholder `.env` — ~464 kB, the real-client path) and `npm run lint` all pass clean. No dependency was added to `package.json`/`package-lock.json` — `playwright` and `pg` were both installed `--no-save` for verification and uninstalled afterward, confirmed by an empty `git diff` on both files. No `.env`, `dist`, or any scratch SQL/harness file was committed.
  **Not checked, stated plainly:** anything against the live Supabase project (no credentials in this environment, owed at 2b); `verify:rls` actually running end to end (see 2a.2); step-by-step list ordering/enforcement, since Lists don't have any UI to create a `step` kind yet — that's Phase 3.
  `TASKS.md` 2a.1–2a.3 ticked with acceptance notes recording the above. Phase 2b is next, and does not start without Adam confirming a live backup exists, immediately before it runs.

---

* **2026-09-11 — Phase 2a reviewed and passed; merged. Phase 2b NOT started — it is waiting on Adam's confirmed live backup.**
  2a landed as `dd22eb6` on `claude/phase2a-migration`. This was the schema chunk and got the hardest look of the build so far, because the criteria's own note says a decided schema change still brings its diff every time.
  **`0002_v2.sql` matches `TASKS.md` § Data model line for line.** `0001_init.sql` is byte-identical; no `position` column anywhere and no `waiting` column on `blocks` (checked against `information_schema`, not by eye); `package.json` and `package-lock.json` untouched. All eight RLS occurrences present — four policies, each with both a `using` and a `with check` — pinned to the **same** owner UUID as `0001`, confirmed by comparing the extracted expressions rather than reading them. Neither migration issues `grant`s, which is consistent: `0001` doesn't either and works live, so Supabase's default privileges carry the new tables the same way.
  **The dry run was re-run, not cited.** A scratch Postgres 16 with an `auth.uid()` stub and representative v1 data — two Areas, a block with three tasks, a block with none, a task in a second Area, two Area-level notes, two tasks already complete — then the committed `0002` applied on top. All 4 tasks survived, all 4 still under their original block in a `flat` list, both completed flags intact; the block with no tasks got **no** list; `tasks.block_id` is gone and `list_id` is `NOT NULL`; existing notes kept `area_id` with `block_id` null. Constraints all bite: self-connection `23514`, duplicate connection `23505`, bogus `lists.kind` and `waiting_entries.kind` `23514`, and `notes_one_parent` rejects both neither-parent and both-parents. Deleting an Area cascaded blocks, lists, tasks, notes at **both** levels, connections, a waiting entry and its picked task — **zero orphans of any kind**.
  **The RLS claim was checked the way the builder said it should be, and the builder was right to say so.** It did not claim `verify:rls` passed — it established that PostgREST genuinely isn't reachable here (dockerd runs, but the image pull is blocked by the egress allowlist) and fell back to proving the policies in SQL, saying plainly that the script's real run is owed at 2b. **That is the correct call and the opposite of v1's false pass.** Re-proved independently across all eight tables under three identities rather than two: no session at all, a stranger carrying a different uid, and the owner. Every table, both stranger cases: `0` rows and writes refused with `42501`. The owner sees the real rows. That third probe matters — a UUID matching no user denies everyone, which fails closed but would equally mean a dead app, and only the owner probe separates the two.
  **2a.3 was driven against the real migrated database, not a fixture.** A PostgREST-to-SQL bridge over Playwright's `page.route` pointed at that same scratch Postgres, with every assertion read back over a **separate** connection rather than from the DOM. 13 checks, all passing: quick-capture makes exactly one `flat` list holding its three tasks with no orphans; a block created with no tasks gets no list, matching the migration; the first task added to a listless block lazily creates exactly one `flat` list; a second task joins that same list rather than making another; toggle and inline rename persist; task delete raises the confirm naming the task (`Delete task "renamed inline"?`) and removes it; an Area-level note saves with `area_id` set and `block_id` null; and deleting the Area leaves `0/0/0/0/0`. No console or page errors. Screenshotted: the popup is v1's exactly — no project canvas, no list UI, no kind picker, nothing of Phase 3. A grep confirms Waiting, Connect and `focused` exist only as types.
  **One observation, narrow, not blocking and deliberately not fixed here.** In `BlockEditPopup.handleAddTask`, if the lazy `createList` succeeds and the `createTask` immediately after it fails, a retry before TanStack Query's invalidation refetch lands would see `list` still null and create a *second* list. Under 2a's "`lists[0]` is the block's list" model the older, empty list wins, so the task would appear lost. It needs a failed write to reach at all, and the Add button is already disabled while either mutation is pending, which closes the common case. Recording it rather than spinning a fix chunk: Phase 3 replaces exactly this "one list per block" assumption with real multi-list handling, and that is where the guard belongs. A one-line local-state fix is available if Adam would rather close it now.
  **Merged** as `ed9a841` into `claude/brave-gates-n66ll3` and `main`; the conflict was again both sides appending to this log, resolved by keeping every entry in the order written. Merged tree builds at 463 kB on the real-client path and lints clean.
  **Phase 2b is next and this reviewer will not start it.** It is the one irreversible step in the build. It needs Adam to take a live backup from the Supabase dashboard and confirm it exists **immediately before** the migration runs — not merely at some point since the phase began. That confirmation has been requested and the build is paused until it arrives. Nothing about it will be assumed or inferred from a builder's summary.

---

* **2026-09-11 — 2b.1's backup gate resolved by Adam: an in-database snapshot, folded into the migration. Written and proven; 2b now waits only on Adam running it.**
  Asked for the backup before 2b, as the criteria require, and got back that a Supabase backup was impossible without paying. **That premise was wrong and saying so mattered more than deferring to it:** Supabase paywalls *scheduled* backups and PITR, but a manual `pg_dump` over the connection string is free on every tier. Adam was told, offered both, and chose the snapshot over `pg_dump`. Recorded as his call, made with the real options in front of him rather than a false constraint.
  **What was built.** `0002_v2.sql` now opens, before it alters anything, by copying all four v1 tables into a `v1_backup` schema. So `v1_backup.tasks` still carries the `block_id` that step 2 drops — the one genuinely destructive act in the migration. Folded into the migration file rather than left as a separate step precisely so it cannot be skipped or run out of order.
  **The schema choice is the security-critical part, not a tidiness preference.** A table sitting in `public` without RLS is served by PostgREST to anyone holding the anon key, and that key ships publicly in the client bundle — a snapshot put there would have handed a stranger every task in the account, which is v1's RLS bug in a new costume. `v1_backup` is outside PostgREST's exposed schema, with an explicit `revoke` as the second layer. **Verified, not assumed:** `anon` gets `permission denied for schema v1_backup` on all four snapshot tables, and `information_schema.table_privileges` shows no grants to `anon` or `authenticated`.
  **Proven by running it, on a rebuilt fixture.** Pre-migration state — 2 areas, 3 blocks, 4 tasks (2 complete), 2 notes — then the modified file applied clean. Snapshot holds 4/4 tasks with `block_id` present and both completed flags; the live `public.tasks` has `list_id NOT NULL` and **errors** on `select block_id` (`column "block_id" does not exist`), so the drop really happened. Every earlier assertion still passes, and RLS on the eight public tables is untouched: 16/16 no-session and stranger probes return 0 rows and `42501`, with the owner still seeing real rows.
  **The restore was rehearsed, not just the backup taken** — the distinction that makes a backup real. For all 4 tasks the snapshot's `block_id` agrees with the `block_id` of the list the task now lives in, and text and completed flags agree; rebuilding v1's task shape from `v1_backup.tasks` alone reproduces 4 tasks with their block links and both completed flags.
  **Written by the reviewer rather than a builder chunk**, and that is worth flagging rather than glossing: it is ~8 lines of exactly-specified SQL Adam had already approved, and the verification is by execution against a scratch database rather than by reading, which is the check that authorship blindness cannot defeat. A builder round-trip would have added a review cycle without adding a real check.
  **An honest limit, recorded in `TASKS.md` 2b.1 too.** This makes *the migration* recoverable. It is not a backup of the database: it lives inside the database it protects, so it does nothing if the project itself is lost or corrupted. `pg_dump` is still the only restore point that survives that, and is still worth two minutes.
  **2b remains Adam's hands.** No session here has or should have DDL access — the anon key cannot run DDL by design, and the alternatives are far broader than a one-time task warrants, exactly as v1 concluded. The file is ready to paste into the SQL editor; verification against the live project follows once he has run it.

---

* **2026-09-11 — The v2 migration reached the live database, and the 2b gate did not hold. Cause under investigation; all `main` pushes are suspended.**
  **What is true regardless of cause.** `0002_v2.sql` is fully and correctly applied to the live project — all four new tables, `notes.block_id` with its one-parent constraint, `blocks.focused`, RLS enabled on all eight tables with one policy each. `tasks` carries `list_id NOT NULL` and no `block_id`. That `NOT NULL` is the evidence the data migration worked: `alter column list_id set not null` would have failed had any task not been given a list. **No data is lost** — the original task→block mapping survives one hop away, through `tasks.list_id → lists.block_id`, because every block with tasks got exactly one flat list. **The snapshot does not exist.** `v1_backup` is absent.
  **Adam raised the possibility that settles it: his GitHub project is connected to Supabase.** If that integration applies `supabase/migrations/*.sql` on push to `main`, then **this reviewer's push applied a schema migration to a live production database** — at 07:31:41, when the Phase 2a merge put `0002_v2.sql` on `main` — before the backup question had even been put to him. The later push at 07:42:07 carrying the snapshot would then have been *skipped*, since Supabase records version `0002` as already applied, which explains the missing snapshot exactly. Adam's manual run afterwards hit `relation "lists" already exists`, which fits.
  **The earlier reconstruction in this log was wrong, and wrong in an instructive way.** It claimed Adam had run a pre-snapshot copy of the file himself. He never said he had; that step was *assumed* because it made the evidence fit, and then written up as though it were established. The evidence was equally consistent with an automated apply, and the right move was to say "these two explanations both fit, here is how to tell them apart" rather than to pick the one that placed the action with the user. A diagnostic on `supabase_migrations.schema_migrations` distinguishes them definitively and has been sent.
  **The gate failed in the one way nobody watched.** Every safeguard built for 2b assumed the migration would reach the database by someone deliberately running it: the backup confirmation, the "2b never starts on this reviewer's say-so", the reasoning that no session here holds DDL access so the irreversible step must pass through Adam's hands. All of that is true and all of it was beside the point if a `git push` is itself a deploy. **Not holding DDL credentials is not the same as being unable to change the database.** That is the lesson, and it generalises past this repo.
  **The permission was real and does not excuse it.** Adam did say "feel free to push things to main branch if needed", and this reviewer pushed to `main` nine times on that basis. But that permission was given about source control, and neither of us knew it also meant "apply migrations to production". The obligation was to establish what a push to `main` actually *does* in this project **before** putting a migration file on it — particularly in a build whose plan of record names one step as irreversible and gates it.
  **Actions taken now, before the diagnostic comes back.**
  1. **All pushes to `main` are suspended** until the integration's status is known. Work continues on `claude/brave-gates-n66ll3` only. A new migration file on `main` could itself auto-apply, which is exactly the mechanism under suspicion.
  2. **`0002_v2.sql` is restored to the byte-exact version that was applied** (`dd22eb6`). Editing an already-applied migration is how a repo and a database quietly diverge — the file claimed to create `v1_backup` while the database had none, and anyone reading the repo would have believed a restore point existed. Its header still says it "is not applied automatically by this repo", which may now be false; the line is left untouched on purpose, because an applied migration is a historical record of what ran, and the correction belongs here and in `TASKS.md` rather than in a rewrite of history.
  3. **The snapshot moves to `0003_v1_backup_snapshot.sql`**, held on the branch and deliberately **not** pushed to `main`. It reconstructs `tasks` in their pre-v2 shape — `block_id` and all — by joining through `lists`, which is exact, since 0002 gave each block with tasks exactly one flat list. Every create is guarded so it is safe to re-run.
  **Still owed:** 2b.2's acceptance against the live data (a read-only verification query has been sent), and a decision from Adam on whether the snapshot is still wanted now that the migration has demonstrably succeeded.

---

* **2026-09-11 — Confirmed: the Supabase GitHub integration applied the migration. Adam wants that behaviour kept; the process moves to fit it.**
  **The evidence is conclusive.** `supabase_migrations.schema_migrations` holds rows for `0001 (init)` and `0002 (v2)`. A script pasted into the SQL editor is not recorded there — only a tracked apply is. So the migrations went in through the integration, and this reviewer's push of `0002_v2.sql` to `main` at 07:31:41 is what applied it to the live project. The hypothesis in the previous entry is confirmed and the reconstruction before it, which placed the action with Adam, was wrong.
  **Adam's position, and it is a fair one:** automatic apply on push is what he wants — the only reason migrations had been run by hand was a belief that no session here *could* run them. **That belief was half right, and the half that was wrong matters.** No session here can query the database: there are no credentials, so nothing here can read a row or check a result. But a session can *cause arbitrary SQL to execute* against production by writing a file into `supabase/migrations/` and pushing. Write without read is a worse asymmetry than plain access, because it is fire-and-forget — the thing that fires it cannot see what it did, which is why every check in this phase has had to route through Adam pasting query output back.
  **The process changes rather than the integration.** Merging to `main` *is* the deploy, so the gate moves to the merge:
  1. A migration file reaches `main` only after it is proven against a scratch database **and** Adam says go. One sentence from him, not a ceremony — but the merge is the irreversible act, so it is his.
  2. Everything that is not a migration merges freely, exactly as before.
  3. **Every builder session from here is told: never add a file to `supabase/migrations/`.** If a chunk looks like it needs one, it stops and asks. A builder cannot be expected to know that creating a file in that directory deploys to production.
  This binds very little in practice: **Phases 3–7 require no migrations at all**, since the entire schema delta was `0002`. The exposure was this phase and is now behind us.
  **2b.2's acceptance, task half, is met** — figures recorded in `TASKS.md`. 20 tasks, all 20 in a flat list, none orphaned; 8 lists for the 8 blocks that hold tasks and none for the other 5; no block with more than one list. There is no pre-migration baseline for the live data, so "every task that existed before still exists" is **not** claimed from counts. It follows structurally: `0002` contains no `delete`, and `set not null` on `list_id` would have failed had any task not been given a list from its own block.
  **A verification bug of this reviewer's own, recorded because this file's most useful entries are about checks that lied.** The combined query reported "completed tasks: 20" out of 20 total — the `where completed` clause was dropped when two scripts were merged into one, so the row was simply the task count again. Caught by noticing the number was implausible rather than by any check. It changed no conclusion, since nothing depended on it, but a plausible-looking number from a broken query is precisely the failure mode `verify:rls` demonstrated twice in v1.
  **Outstanding:** the RLS half of 2b.2 — `verify:rls` over all eight tables against the live project — which needs the anon key in a session that can run it; and Adam's go-ahead on `0003_v1_backup_snapshot.sql`, which is proven against a production-like database but not pushed, because pushing it is applying it.

---

* **2026-09-11 — Adam approved applying `0003`; this session cannot push it, and the anon key is not recoverable here.** Two blockers, both external to the work itself.
  **The push to `main` was refused by this session's permission classifier**, not by any rule in this repo. In Adam's setup that push is what deploys, so the refusal is well aimed even though he had said go — and it was left alone rather than routed around through the GitHub API, since the blocked capability is "execute SQL against production" and that is the kind of block worth respecting on a second attempt as much as a first. Three routes offered: he merges the branch himself (cleanest — the integration then applies and records `0003`, keeping repo and database in step), he pastes the file, or he grants the permission. `0003`'s creates are all `if not exists`, so a hand-run now and an integration apply later are compatible: the second is a no-op.
  **The anon key is genuinely absent, not withheld.** Asked to just take it if it was reachable, this session looked: `.env` holds only the placeholder written here; the real key lived in a *previous* container's `.env`, which this file already records as ephemeral and which is destroyed; nothing resembling a JWT is in any commit (`.env` is gitignored, and the `eyJ` matches in `package-lock.json` are fragments of npm sha512 integrity hashes); and no deployed frontend is referenced anywhere, so there is no public bundle to read it out of.
  **One useful negative result, though: the project is reachable from this container.** `https://ieszecmiijxkcrxirwuk.supabase.co/rest/v1/` returns a proper PostgREST `401 — No API key found in request`, so the egress allowlist problem that produced v1's false `verify:rls` pass is not present this time. `verify:rls` can run against all eight live tables the moment the key is supplied.

---

* **2026-09-11 — Phase 2 complete. `verify:rls` passes against the live project for all eight tables; the snapshot's isolation confirmed from outside. Phase 3 started.**
  Adam merged the branch (PR #1), so `0003_v1_backup_snapshot.sql` is on `main` and the integration has it, and he supplied the anon key.
  **The key was checked before it was used**, as v1 did: its JWT decodes to `ref: ieszecmiijxkcrxirwuk`, `role: anon`, unexpired. The `role` claim is the one that matters — a `service_role` key bypasses RLS entirely, so running `verify:rls` with one would produce a confident pass that proved nothing at all.
  **`verify:rls` passes for real, on all eight tables.** `areas`, `blocks`, `tasks`, `notes`, `lists`, `connections`, `waiting_entries`, `waiting_entry_tasks` — each returns no rows with no session and refuses writes with PostgREST `42501`; signup is refused with `422 signup_disabled`. The four *new* tables had until now been proven only against a scratch database, which is precisely the gap that hid v1's "any authenticated user" policy.
  **The snapshot's isolation was verified from the outside, which is better than the scratch test that preceded it.** Using the public anon key against the real project, `Accept-Profile: v1_backup` returns `PGRST106 — Invalid schema: v1_backup. Only the following schemas are exposed: public, graphql_public`, and `v1_backup_tasks` is not addressable from `public` either. The control matters as much as the result: `public.tasks` returns `[]` rather than an error on the same path, so the refusal is genuine isolation rather than a malformed request — the distinction v1's `UNREACHABLE` fix exists to force.
  **One thing this session cannot confirm and is not claiming:** that `0003` actually created `v1_backup` in the live database. The anon role has no access to that schema by design, and `supabase_migrations` is equally out of reach, so the snapshot's *existence* is unverifiable from here — only its *inaccessibility* is. A one-line query from Adam closes it. Recorded rather than assumed, since "the migration was on `main` so it must have applied" is exactly the reasoning that went wrong earlier today.

---

* **2026-09-11 — Phase 3 is overrunning badly and has pushed nothing. Escalated to Adam: let it run, or stop and re-scope.**
  Builder `session_01KZWQ8BZTue7AxqnFKdpyX5` started 08:09 and at 11:37 was still running with **nothing on the remote** — `claude/phase3-project-canvas` does not exist. That is **3 hours 28 minutes** against 13–20 minutes for every previous chunk, which puts it squarely on the always-escalate list under "changes scope, cost, or timeline versus `TASKS.md`".
  **It is slow rather than merely busy, and the token figures are what show it.** 83.8k output tokens and $3.94 so far — *fewer* tokens than Phase 2a produced (106.6k, $6.54) in about twenty minutes of wall clock. So this is not a large chunk grinding productively through a large amount of work; something is taking a long time per unit of output. Its own status line at the time of the poll read "checking for running relay processes and killing any stragglers", which is test-harness wrangling, not feature work. It is genuinely alive — the record updated at the moment it was polled, not hours earlier.
  **The exposure is v1's Phase 0 failure repeating.** That builder was cut off mid-work with nothing pushed, and the log records the consequence plainly: the branch never reached the remote, the container disconnected, and what it had built was gone. Three and a half hours of unpushed work is the same shape of risk, and it grows with every minute.
  **Nothing could be done about it from here, which is worth recording as a limitation rather than a footnote.** Cloud sessions are not reachable as peers — `ListAgents` sees none, unchanged since v1 — and no `send_message` tool is available in this session, so the builder cannot be asked to commit and push what it has. `interrupt_session` exists but was deliberately **not** used: it would stop the turn without any way to follow up with an instruction, leaving the same unpushed work in a now-idle container. That is strictly worse than letting it continue. The only lever available is Adam's.
  **The question put to him:** let it run, or stop it and re-scope Phase 3 into smaller chunks. Phase 3 is eight items spanning a resizable popup, a second-level freeform canvas, list creation with a fixed kind, full task CRUD, ordering enforcement in both directions, a cross-project Waiting check, project-level notes and list deletion — plausibly too much for one session, and the natural split is 3.1–3.4 (the canvas and basic CRUD) from 3.5–3.8 (the ordering rule, the Waiting check and notes).
  **A process note for whatever comes next:** a builder should be told to commit and push early and often, rather than only at the end. Every chunk so far has pushed once, at completion, which is fine when a chunk takes fifteen minutes and catastrophic when it takes four hours.

* **2026-09-11 — Adam's answer on the Phase 3 overrun: let it run.** No re-scope, no interrupt; the reviewer keeps polling at 15-minute intervals and reviews the moment a branch appears. The unpushed-work risk is accepted knowingly — it was put to him plainly, with v1's Phase 0 loss named as the precedent. Nothing else changes: the review checklist for Phase 3 stands, and 3.6's two-directional ordering rule is still the first thing to be tested.

---

* **2026-09-11 — Phase 3 (project canvas) built and verified. No file added to `supabase/migrations/`.** Builder for `claude/phase3-project-canvas`, branched from `claude/brave-gates-n66ll3`. The migration-incident warning from Phase 2's build log was read and held to: nothing in this phase needed a schema change, and nothing was added — `0002_v2.sql` already created `lists`, `tasks.list_id`, and `notes.block_id`, which is the entire schema surface 3.1–3.8 needed.
  **What changed and why**, beyond the per-file notes in this file's own "Phase 3 — what was built" section above: `Popup.tsx` gained two *optional* props so the block popup alone could become a resizable window without touching the shared shell every other popup in the app depends on — every other caller's output is unchanged, confirmed by leaving their call sites untouched rather than by a diff review alone. `BlockEditPopup.tsx` stopped being "the block's one flat task list" and became a project-canvas host; the 2a.3 stepping stone it replaces (`lists[0]` is the list, lazily created) is gone in full, not patched around, which is also what closes the known create-path race flagged at the end of 2a's review — there is no longer a lazy-list-creation code path on the interactive add-task flow for a retry to race against.
  **3.5 needed a design decision the plan didn't spell out down to the SQL, and it's worth recording:** whether an emptied Waiting entry's removal happens via the database's own cascade or a second explicit delete. It's the latter, deliberately — `waiting_entry_tasks.task_id` cascades on task delete (so a pick disappears for free), but `waiting_entries` itself has no FK back to its picks, so an entry that just lost its last pick would otherwise sit there empty forever, exactly the "vacuous Ready" trap 4.4's own reasoning already worried about. `removeEmptiedWaitingEntries` closes that explicitly, called right after the task delete succeeds, using the pick-count read from *before* the delete (an entry with exactly one pick, which is the one about to be removed, is "last").
  **Verification had no live Waiting data to exercise, since Phase 4 hasn't shipped the only path that creates any** — handled by seeding `waiting_entries`/`waiting_entry_tasks` directly by SQL against the scratch database, the same "inert on its own build, must be correct now" posture the plan itself names for 3.5. That is real coverage of `findWaitingHolds`/`removeEmptiedWaitingEntries` against rows shaped exactly as Phase 4 will produce them, not a skip.
  **The verification harness, in brief** (full detail in this file's "Phase 3 — what was built" section): a scratch Postgres 16 with `0001`+`0002`+`0003` applied and a stub `auth.uid()`; a small relay translating `/rest/v1/*` into real parameterized SQL under `SET LOCAL ROLE authenticated` with the owner UUID as the JWT claim, so RLS was genuinely enforced rather than bypassed by a superuser connection; a fetch-interception init script plus a seeded `localStorage` session, the same pattern every phase since Phase 1's fix chunk has used; driving the real `vite dev` app in headless Chromium. 33/33 checks passed, including the exact step-list refusal sequence the plan's own acceptance bar spells out in both directions, alongside the same operations on a Flat list in the same project refusing nothing. Two false failures were found and fixed in the *test*, not the app, both worth recording since this file's stated purpose is catching that distinction: `text=` locators don't match a controlled `<textarea>`'s value (React sets `.value` directly rather than a child text node), so task-name lookups were switched to the checkbox's `aria-label`; and a Note happened to land visually on top of a List's "Delete list" button at the harness's own viewport-centred placement (freeform canvas, no anti-overlap logic, exactly per spec) — switched that one click to `dispatchEvent('click')`, which targets the element directly rather than by screen coordinates. Neither reflects app behaviour a real user would find surprising, and neither changed what was asserted about the database afterward.
  **Not checked, stated plainly:** the live Supabase project (this container had no `.env` at session start); `verify:rls` (no schema changed this phase, so nothing new to verify there); anything from Phase 4 onward (Waiting UI, Focused, Connect, Filter) — out of scope. `npm run build` (201 kB, no `.env` present — see the Phase 0/1 entries above for what that figure does and doesn't prove) and `npm run lint` both pass clean. No new dependency in `package.json`/`package-lock.json` — `playwright`/`pg` were installed `--no-save` for the harness and never committed; no `.env`, harness script, or scratch SQL was committed either.
  `TASKS.md` 3.1–3.8 ticked with acceptance notes recording the above. Phase 4 (Waiting) is next, and needs Phase 3's Lists to pick tasks from.

---

* **2026-09-11 — Phase 3 reviewed and passed; merged. Five of eight chunks are done.**
  Landed as `292deea` on `claude/phase3-project-canvas` after 3h37m and $9.17 — the overrun Adam chose to let run. It pushed only at the very end, which is exactly the exposure that was flagged: had the container died at 11:46 the whole thing would have been lost, as v1's Phase 0 was.
  **Verified independently, 18 checks in Chromium** against a real Postgres carrying `0001`+`0002`+`0003`, with every assertion read back over a *separate* connection rather than from the DOM. The builder reported 33/33 of its own; that was not taken on trust, because every builder in this build so far has reported a clean verification at least once while shipping something real.
  **3.6 holds in both directions, which is the whole point of it.** With a three-task step list: completing task 2 while task 1 is incomplete is refused (`disabled: [false, true, true]`); completing 1 works and then opens 2 but not 3 (`[false, false, true]`); **un-completing task 1 while 2 is complete is refused**; un-completing 2 — the last completed — works, and task 1 becomes un-completable again. That second direction is the half that is normally missed, and missing it would allow task 1 incomplete beneath a complete task 2, the exact state the rule exists to prevent. A Flat list in the same project gates neither direction: all three checkboxes enabled, and completing the *last* task with the other two incomplete works.
  **The rest.** `resize: both` on the popup with nothing persisted (3.1). A note and a list each dragged by (260, 150) and (120, 90) and persisted those exact deltas (3.2). List kind fixed at creation — `lists.ts` exports no update path for `kind` at all, so it is unchangeable by construction rather than by UI omission (3.3). A project note saved with `block_id` set and `area_id` null, and **`NoteCard` is byte-identical** — 2a.3's `NoteParent` union did its job and Phase 3 is simply its first caller, with no fork (3.7). List delete through the confirm dialog took its three tasks with it (3.8). No console or page errors.
  **3.5 could only be read, not exercised**, and that is expected: nothing can populate `waiting_entries` until Phase 4. It is correct — `findWaitingHolds` queries picks with no block scoping, so it sees entries on *any* project; it joins through to the block name so the confirm can say which project is waiting; `isLastPick` is computed before the delete, so an entry that loses its last pick is removed afterwards with no second confirmation. It should not need revisiting when Phase 4 ships.
  **The old lazy-list defect is closed by removal, not by a guard** — `BlockEditPopup` no longer creates lists at all; `ListCreatePopup` does, explicitly. The double-create race had no mechanism left to exploit.
  **Boundaries held, including the new one.** Nothing under `supabase/migrations/`. No Waiting, Focused, Connect, Filter or content-marker UI; Phase 4–7 features exist only as types. No reorder affordance anywhere. Merged tree builds at 470 kB on the real-client path and lints clean.
  **Phase 4 (Waiting) is next**, and it is where 3.5 stops being inert.

---

* **2026-09-11 — Phase 4 (Waiting) reviewed and passed; merged. Six of eight chunks done.**
  Landed as `8d10d03` on `claude/phase4-waiting` in 24 minutes and $6.83 — and it **pushed a working commit at 22 minutes**, so the push-early instruction added after Phase 3 worked exactly as intended. It then went idle at 12:28 "waiting on harness completion" **without updating `TASKS.md` or `CONTEXT.md`**, which its brief required; the code was complete but the chunk was not. Those updates were written by this reviewer instead of spending another builder session on documentation, and `TASKS.md`'s Phase 4 acceptance note says so plainly rather than reading as the builder's own.
  **Verified with 19 independent checks** in Chromium against a real Postgres carrying `0001`+`0002`+`0003`, every assertion read back over a separate connection.
  **4.4's two vacuous-truth guards both hold, and the second one could only be proven from outside the client.** Zero entries → not Ready. A picked-task entry holding **zero picks** → not Ready — that row state was created directly in SQL, because it is what a cascaded task delete leaves behind and no UI path produces it. So Ready genuinely does not depend on 3.5 having tidied up, which is the whole reason the clause exists.
  **4.5 refuses with the actual reason**, not a generic message: `Can't change status off Active while still Waiting: a free-text entry ("call the supplier back")`. It also gates on `describeOutstanding()` being non-empty rather than on `!isProjectReady()` — a distinction that looks pedantic and is not: those differ exactly for a project with **no** entries, and gating on `isProjectReady` would have trapped every ordinary project in Active forever.
  **4.3's picker is scoped at the query level** — `.eq('area_id', …).neq('id', ownBlock)` — so another Area's tasks cannot appear by construction rather than by UI filtering. Driven: a same-Area neighbour's two tasks listed, own task absent, other-Area task absent.
  **3.5 is live and works end to end, having been inert since Phase 3.** Deleting a picked task warns `It's currently a pick in a Waiting entry on Waiter`, removes the pick (2→1) with the entry standing, and when the last pick goes the entry goes with it (picks 1→0, entries 1→0) with no second confirmation.
  **Looked at it, not just measured it.** Side by side, the Waiting project renders `oklch(0.882…)` against an Active neighbour's `oklch(0.623…)` — unmistakably dimmer, which is what v1's marker defect failed at. The card reads "Waiting — 2 entries" folded, expands to list both Done picks struck through **and still present**, with the free-text entry below and no Ready badge, correctly.
  **Three of this reviewer's own harness bugs are worth recording, because two would have produced a false defect report.** The PostgREST bridge silently dropped `neq.` filters, so the app's real same-Area exclusion never reached SQL and the project's own task appeared in the picker — that looked exactly like a 4.3 violation and was not. It also dropped `columns=`, a bulk-insert parameter. And an earlier run "passed" two picker assertions **vacuously**: the popup defaults to Free-text mode, so no tasks were listed at all, and "the forbidden task is absent" was trivially true of an empty list. The bridge now throws on any operator it does not recognise rather than ignoring it, which is the fix that generalises: **a filter silently dropped is a check that silently proves nothing** — the same failure as v1's `verify:rls`, in a new costume.
  **One minor observation, not a defect.** The 4.5 refusal reaches the screen through `pushError`, which prefixes `Couldn't save:` — wording inherited from v1's mutation-error banner. A refusal is not a failed save. Cosmetic, and not worth a fix chunk on its own; worth folding into a later one if anything else in that area is touched.
  **Boundaries held:** nothing under `supabase/migrations/`, no `waiting` column added or queried, no Focused/Connect/Filter built early. Merged tree builds at 478 kB on the real-client path and lints clean.
  **Phase 5 (Focused) is next** — it needs Waiting to exist, since the two are mutually exclusive.

---

* **2026-09-11 — Phase 5 (Focused) built and verified. Seven of eight chunks done.** Builder for `claude/phase5-focused`, branched from `claude/brave-gates-n66ll3` (Phase 4 built, reviewed and merged). Pushed a working, typechecked, lint-clean commit at the point where the feature code compiled — before verification started, not after — per the standing instruction to push early and often; a second small commit followed to rename a UI label a naming collision surfaced during verification.
  **No schema change, and none was needed.** `blocks.focused` already existed from `0002_v2.sql`; nothing was added to `supabase/migrations/`. `useUpdateBlock`'s mutation type grew a `focused?: boolean` field, which it already wrote generically alongside `name`/`status` — the only other new query is `useFocusedBlocks()`, a cross-Area read via the `blocks -> areas(name)` FK embed, the same embed pattern `waiting.ts` already used for `blocks(name)`.
  **5.2's mutual exclusion is two guards, not one, and both were driven against real database state in both directions — including the "clearing the blocker then allows the other" half the brief specifically called out.** Focusing a project holding a Waiting entry (Beta) was refused, `blocks.focused` confirmed still `false` by query, with a banner naming Waiting; opening the "+ Waiting entry" popup on a Focused, entry-free project (Epsilon) was refused *before the popup rendered at all* (confirmed by counting `text=New Waiting entry` — zero), with a banner naming Focused, and `waiting_entries` confirmed still empty by query. Then, deliberately, each blocker was cleared and the other direction re-tried: unfocusing Epsilon let a Waiting entry be created on it moments later (row appeared, text confirmed by query); deleting Beta's Waiting entry through its own confirm dialog (4.9, reused) then let Beta be Focused (`focused` became `true`, confirmed by query). Both directions, both before and after, all against the database rather than the UI's own claim about itself.
  **The Focus screen's cross-Area jump was proven as genuinely cross-Area, which took deliberate test design, not just a click-and-check.** The first attempt (not shown in the final script) would have clicked a project in the Area that was *already* active, which is a same-Area click wearing a cross-Area test's clothes — it would have passed even if the jump logic were entirely broken. The corrected version explicitly makes Work active, then clicks Delta (Home) from the Focus screen and confirms the Home tab gained the active-tab class *and* Delta's own popup opened (name field reading "Delta") — and separately, with Home active, clicks Gamma (Work, seeded at `x=3000,y=2500` specifically to start off-screen) and confirms the canvas's scroll container measured `scrollLeft: 1880, scrollTop: 1738` afterward — nowhere near its `(0,0)` rest position, which is what actually proves the scroll-into-view code ran rather than merely that Gamma was present somewhere on an unscrolled, overflowing canvas.
  **5.4's outline and 4.8's Ready badge were checked side by side in the same seeded database, not just described as different.** A Focused block (Delta) measured `outlineStyle: "solid", outlineWidth: "2px"` by computed style; a Waiting+Ready block built alongside it (Zeta — one picked-task entry, its task completed) measured `outlineStyle: "none"` and showed the `[aria-label="Ready"]` badge instead, with Delta showing zero such badges. Screenshotted together, not just asserted: Delta reads as a plain blue Active block with a purple ring; Zeta reads as dimmed-blue with a folded Waiting card and a green check — unmistakably different, which is the entire point of 5.4's deliberate contrast with 4.8 (Ready is a badge because it must not blend into a colour state; Focused is the outline).
  **A test-harness bug, not an app bug, was caught and fixed mid-run, which is exactly the distinction this file's build-log entries exist to keep straight.** After the first refused mutual-exclusion attempt, the resulting error banner (fixed to the top of the page, above everything, by design — "a failed save is worth noticing late") intercepted every subsequent click on the Area tabs underneath it, timing out the test. The fix was dismissing the banner in the harness after each refusal check, not touching `ErrorBanner.tsx` — the banner's own behaviour is correct and unrelated to Phase 5.
  **One real, if minor, UI improvement came directly out of verification.** The header's new Focus-screen toggle and the in-popup Focused toggle both read "Focus" in one state — harmless to a sighted user since they're nowhere near each other on screen, but indistinguishable by accessible name to anything reading the accessibility tree, which is exactly how the verification harness (and a screen reader) finds buttons. Renamed the header control to "Focus screen"; the popup's own toggle keeps its shorter "Focus"/"Focused" label. Caught by the harness failing to disambiguate two same-named buttons, not by manual inspection.
  **The verification harness itself, in brief** (full detail in this file's "Phase 5 — what was built" section above): a scratch Postgres 16 with `0001`+`0002`+`0003` applied and a stub `auth.uid()`; a hand-rolled relay translating `/rest/v1/*` into real parameterized SQL under `SET LOCAL ROLE authenticated` with the owner UUID as the JWT claim — genuine RLS, not a superuser bypass — that **throws on any filter operator it doesn't implement** and **never treats `select`/`order`/`limit`/`offset`/`columns`/`on_conflict` as a filter**, both lessons taken directly from Phase 4's own review entry above rather than relearned the hard way. 29/29 checks passed, driving the real `vite dev` app in headless Chromium. Zero browser console errors. Two screenshots taken.
  **Not checked, stated plainly:** the live Supabase project (this container had no `.env` at session start; a placeholder one was written for the `vite dev` verification run and deleted before finishing, the same pattern every phase since Phase 3 has used); Phase 7's Filter, which doesn't exist yet, so 5.5's "not coupled to Filter" is a property of what wasn't built (nothing here reads or references any filter state) rather than something exercised directly; keyboard/screen-reader navigation beyond the accessible names the harness itself relied on to drive the UI.
  `npm run build` (201.72 kB, no `.env` present, consistent with every prior phase's own figure under the same condition) and `npm run lint` both pass clean. No dependency added to `package.json`/`package-lock.json` — `playwright`/`pg` were installed `--no-save` for the harness and uninstalled afterward, confirmed by an empty diff on both files. No `.env`, harness script, or scratch SQL was committed.
  `TASKS.md` 5.1–5.5 ticked with acceptance notes recording the above. **Phase 6 (Connect) is next**, and is the first phase whose layout has to account for a block's real height — which, after Phase 4 and 5, can now carry a Waiting card and/or a Focused outline.

---

* **2026-09-11 — Phase 5 (Focused) reviewed and passed; merged. Seven of eight chunks done.**
  Landed in 20 minutes and $6.86 across three commits — pushed incrementally, **and this time the builder finished its own documentation**: `TASKS.md` 5.1–5.5 ticked and a `CONTEXT.md` entry written, which Phase 4's builder had skipped. Adding "finish the chunk, including the documentation" to the brief was enough.
  **Verified with 13 independent checks** in Chromium against a real Postgres, every assertion read back over a separate connection.
  **5.2's mutual exclusion works in both directions, which is the item that is normally half-built.** Turning on Focused while Waiting is refused with `Can't turn on Focused while Waiting: turn off Waiting first`; adding a project's *first* Waiting entry while Focused is refused with `Can't turn on Waiting while Focused: turn off Focused first`. Each refusal names the other state, nothing is swapped or cleared on the user's behalf, the blocking Waiting entry survives the refusal untouched, and clearing either one then permits the other. The guard is against **entry existence** via `isProjectWaiting`, not an invented flag — there is no `waiting` column and none was added.
  **The other direction's placement is a good judgment call worth noting.** Adding the *first* entry is precisely "turning Waiting on" (4.1), so that is the single moment the check belongs; once a project already holds entries it cannot have been Focused, because this same guard refused that transition. One check, in the right place, rather than a scattering of defensive ones.
  **5.3 clears Focused in the same write as the status change** — `if (status === 'done' && blockFocused) changes.focused = false` — rather than a second mutation a failure could leave half-applied. Confirmed in the database, not the UI: `Gamma: status=done, focused=false`.
  **5.5 the Focus screen** lists all three Focused projects grouped under both Area headings; clicking the cross-Area one switches Area, opens its popup, and genuinely scrolls the block into view — it was seeded at `y=900` and ended at `top:864, inView:true`, so that is a real scroll rather than a block that happened to be visible. Marking it Done removes it from the screen. **5.4** renders `outline: solid 2px`, which stays distinct from 4.8's Ready badge — and the two can never collide anyway, since 5.2 makes Focused and Waiting mutually exclusive.
  **Three harness bugs of this reviewer's own, all in the PostgREST bridge, and the third is the most instructive of the whole build.** The bridge quoted the `*` in `select('*, areas(name)')` into a column named `"*"`; and its embed subquery used the alias `x`, which **collides with `blocks.x`** — the block's own position column — so Postgres resolved `row_to_json(x)` to a `double precision` and errored. The Focus screen therefore rendered empty and looked exactly like a broken 5.5. It was correct all along. Phase 4's embeds had worked only because `waiting_entries` has no `x` column, so the collision never fired. **That is the pattern to carry: a test harness that is subtly wrong produces a confident, specific, entirely false defect report** — and across Phases 4 and 5 this reviewer's harness has now manufactured three of them. Reading the generated SQL directly, rather than trusting the harness's own verdict, is what caught each one.
  **A related trap, twice now:** two assertions passed **vacuously** — "the forbidden task is absent" against an empty picker, and "both Areas appear" matching the header tabs rather than the Focus screen's own groupings. An absence check is worthless unless something asserts the list is populated first.
  **Boundaries held:** nothing under `supabase/migrations/`, no Connect/Filter/content-marker built early, no "Today", no next-step pointer. Merged tree builds at 481 kB on the real-client path and lints clean.
  **Phase 6 (Connect) is next — the last feature phase**, and the one whose auto-layout has to account for how tall a block is now that it can carry a Waiting card.

---

* **2026-09-11 — Phase 6 (Connect) built and verified. Eight of eight chunks done — the last feature phase.**
  Builder for `claude/phase6-connect`, branched from `claude/brave-gates-n66ll3` (Phase 5 built, reviewed and merged). Pushed a working, typechecked, lint-clean commit as soon as the feature code compiled, before verification started, per the standing "push early and often" instruction.
  **No schema change, and none was needed.** `connections` already existed, in full, from `0002_v2.sql` (self-edge check, `unique(source_id, target_id)`, cascading FKs on both endpoints) — the phase brief's own framing ("Phase 6 needs no schema") held exactly. Nothing was added to `supabase/migrations/`.
  **6.3's acyclicity check runs entirely before the network call, not as error handling after one.** `src/lib/layout.ts`'s `wouldCreateCycle` is a plain directed BFS from the candidate target over the Area's existing connections, checking whether it can reach the candidate source; `checkConnection` in `src/lib/connections.ts` calls it (plus self-edge and duplicate checks) from the UI, and only calls `useCreateConnection().mutate` if it returns clean. A refused cycle attempt therefore cannot write a row by construction — there is no code path where the mutation is even attempted — rather than relying on catching Postgres's own eventual constraint violation (which, per the phase brief's own reminder, wouldn't even fire: the database happily accepts both directions of a 2-block cycle; only the client-side check catches it at all).
  **The layout algorithm (6.5/6.7) is the genuinely hard part of this phase, and where the most design time went.** `computeConnectionLayout` has two branches. Merging two previously separate components: the larger (by block count, ties to the upstream/source side — which also covers two never-before-connected blocks meeting for the first time, a size-1/size-1 tie) is **never touched**, full stop, in any circumstance; the smaller is rigidly translated — its own existing internal shape preserved exactly, not recomputed — so its bridge endpoint lands one column-pitch from the winning side's, nudged a row if that exact slot is already occupied. Adding an edge *within* an already-single component (a shortcut, a fork-closing edge) is a case 6.7 never names; resolved conservatively — shift only the new edge's target and everything downstream of it, only by the minimum needed to keep it right of its source, leaving anything upstream or on an unrelated branch untouched, and doing nothing at all if the existing layout already satisfies the new edge. This is written up as a documented interpretation of a genuine spec gap in `TASKS.md`'s 6.5 and `src/lib/layout.ts`'s own comments, specifically because the brief calls out that an under-determined case should be said so rather than silently guessed at — the reasoning for rejecting a full global recompute (it can force blocks with no relation to the new edge to move, which the spec says nothing about) is recorded there rather than just implemented and left unexplained.
  **The diamond test is the one the phase brief calls out as the case that actually distinguishes a correct longest-path implementation from a plausible-looking bug**, and it was hand-traced against the algorithm before ever touching a browser: `A→B, A→C, B→D, C→D`, both paths to `D` length 2, so `D` must land in the third column (`A.x + 2×220`), not the second (`A.x + 220`, what a naive "one edge at a time, ignore the other branch" implementation would produce if it let the *second* incoming edge to `D` — `C→D` — silently overwrite the *first*'s placement instead of recognizing it as already satisfied). The browser run confirmed the hand trace exactly: `D.x = A.x + 440`, `B` and `C` both at `A.x + 220` on different rows. Screenshot: `/tmp/phase6-diamond.png`.
  **A real harness bug, caught and fixed mid-run, and instructive about testing a coordinate-space feature specifically.** The first version of the connection-line-click test computed the line's on-screen midpoint from the *rendered card's* bounding box center — but `ConnectionLines.tsx`'s line endpoints use a fixed `BLOCK_HALF_HEIGHT = 30` constant (matching `Canvas.tsx`'s existing approximation pattern), while the actually-rendered card in the test was only 36px tall (half-height 18), a 12px mismatch that put every click 12px off the real line. The fix wasn't a bigger click target in the app (the invisible hit-line is already 14px wide, deliberately generous) — it was computing the test's click point the same way the app computes the line itself: from the blocks' stored `x`/`y` plus the same `+160`/`+30` offsets, transformed through the SVG's own screen origin, not from a rendered element's visual center. A second, smaller harness bug in the same area: the confirm dialog's buttons are labeled "Cancel"/"Confirm", not "Delete" — a copy-paste assumption from a different part of the test, caught by the same failed run.
  **6.6's rigid-component drag is asserted the way the brief specifically demands: numeric equality of all four deltas, not "everything moved."** Dragged one block of an already-established 4-chain by `(150, −60)`; all four blocks' `(new − old)` deltas compared pairwise for exact equality, and the pitch between consecutive blocks re-checked afterward to confirm the shape itself — not just each block's absolute position — survived intact.
  **6.7's "never its position" was checked at the strictest possible reading: byte-identical coordinates, not "approximately unchanged."** Connecting a lone block to the (already-dragged) 4-chain, then separately deleting one of its connections, both checked every un-involved block's `x` and `y` for exact equality against its pre-action value — the kind of check that a rounding bug or an accidental re-layout of the "unaffected" side would fail immediately, and a looser "still looks like a chain" check would not have caught.
  **Verification harness, in brief** (full detail in `CONTEXT.md`'s Phase 6 "what was built" section above): scratch Postgres 16, `0001`+`0002`+`0003` applied, a stub `auth.uid()` reading `request.jwt.claims`; a hand-rolled relay translating `/rest/v1/*` into real parameterized SQL under `SET LOCAL ROLE authenticated` with the owner UUID as the JWT claim — genuine RLS, not a superuser bypass — throwing on any unimplemented filter operator and never treating `select`/`order`/`limit`/`offset`/`columns`/`on_conflict` as a filter, both lessons carried forward verbatim from earlier phases' own review notes rather than relearned. Seeded 3 Areas (a 3-block cycle-attempt graph, a 4-block diamond, and the 5-block chain-then-merge-then-delete-then-orphan narrative) directly by SQL, then drove every connect/drag/delete through the real UI in headless Chromium. **30/30 checks passed.** Zero browser console errors across the entire run.
  **Not checked, stated plainly:** the live Supabase project (this container had no `.env` at session start; a placeholder one was written for the `vite dev` verification run and deleted before finishing, the same pattern every phase since Phase 3 has used); a connection line's exact pixel anchor against a card whose rendered height varies with content (Waiting card, wrapped name) — the app's own `BLOCK_HALF_HEIGHT` constant is a fixed approximation, same shape as `Canvas.tsx`'s pre-existing `NEW_BLOCK_HALF_HEIGHT`, cosmetic only and out of scope per "no visual design pass"; a merge whose *losing* component's own internal shape attaches to a non-boundary node of its own graph (every scenario actually run only ever merges a lone block into a larger shape, which is also the only shape the phase's acceptance bar names) — `computeConnectionLayout`'s rigid-translation choice for that broader case is a documented interpretation, not something this run exercised; Phase 7's Filter and content marker, which don't exist yet.
  `npm run build` (build succeeds, no `.env` present at commit time) and `npm run lint` both pass clean. No dependency added to `package.json`/`package-lock.json` — `playwright`/`pg` were installed `--no-save` for the harness and are not referenced by any committed file. No `.env`, harness script, or scratch SQL was committed.
  `TASKS.md` 6.1–6.9 ticked with acceptance notes recording the above. **Phase 7 (Filter and the content marker) is next — the last phase of the whole build.**

---

* **2026-09-11 — Phase 6 reviewed. The code is good; the *plan* turns out to be internally inconsistent. Escalated to Adam; not merged, Phase 7 not started.**
  Landed as `fbe9a00` + `4386500` on `claude/phase6-connect`. Three hours of wall clock but only $1.90 of spend for most of it — the account usage limit Adam hit throttled the builder too, so this was not Phase 3's slowness repeating. It pushed early, finished its own documentation, added nothing under `supabase/migrations/`, and kept Connect and Waiting structurally independent (neither `connections.ts` nor `waiting.ts` references the other). Build 487 kB on the real-client path, lint clean, connection lines render left-to-right with arrowheads, no console errors.
  **The builder deliberately made `src/lib/layout.ts` free of React and Supabase "so the algorithm can be re-derived in review". That paid off immediately** — the whole graph layer could be compiled standalone and tested directly, which is how everything below was established.
  **A correction to this reviewer's own earlier claim, made twice and wrong both times.** The brief and a check-in both asserted the *diamond* (`A→B, A→C, B→D, C→D`) is the case distinguishing longest-path from shortest-path layering. It is not: both paths to D are length 2, so the two algorithms **agree**. The case that actually separates them is a **skip edge** — `A→B, B→C, A→C`, where longest gives C column 2 and shortest gives column 1. The builder tested the diamond as instructed and read it correctly for what it does prove; nobody tested the distinguishing case, because the brief sent everyone at the wrong shape.
  **Tested properly, the algorithm passes the cases that matter.** 11/11 direct checks: the skip edge puts C in column 2 in **all three** build orders; a chain-plus-shortcut puts D in column 3; the diamond is right; a cycle-closing edge is refused while a transitive one is allowed; a merge leaves the larger component byte-identical; a 1-vs-1 tie goes to the source; components are undirected.
  **But a property test over 385 random DAGs shows the implementation is not the algorithm 6.5 names.** 135 blocks landed in a column other than their longest-path depth. It is incremental placement — shift the new edge's target and its descendants just enough — not layering. Within a component, 85 of 534 comparisons had a *different internal layout depending on the order the edges were added*.
  **Before reporting that as a defect, the obvious question: is the result merely different, or actually wrong? Different.** Across 343 trials there were **zero** left-to-right violations — every edge always runs target-right-of-source, so 6.1's "comes before" never breaks and nothing ever renders incoherently.
  **And then the finding that actually matters: 6.5 and 6.8 cannot both be true.** Demonstrated, not argued. Build `A→B→C` (columns 0,1,2) and delete `A→B`. On the remaining graph, longest-path layering requires B at column 0 and C at column 1 — but **6.8 says removing a connection repositions nothing**, so B stays at 1 and C at 2. The layout necessarily stops being longest-path the first time any edge is deleted. **6.5 states an invariant that 6.8 forbids maintaining.** The builder's incremental approach is not a shortcut around the spec; it is close to the only thing that can satisfy 6.6, 6.7 and 6.8, which are the clauses carrying concrete acceptance criteria.
  **So this is escalated as a plan problem, not a build problem.** The builder had already flagged a narrower gap honestly — that an edge added *within* an existing component is unnamed by 6.7 — and documented its interpretation in `TASKS.md` and in code. It did not notice the wider inconsistency, which is fair: it is only visible from outside, comparing 6.5 against 6.8.
  **What is being asked of Adam:** whether to accept the implemented behaviour and correct 6.5's wording to describe the initial shape rather than a maintained invariant, or to keep 6.5 literally and relax 6.7/6.8 so a global recompute is permitted to move blocks. The first needs no code change; the second is a real rewrite of the phase.

* **2026-09-11 — Adam's answer on the 6.5/6.8 contradiction: accept the implemented behaviour, correct 6.5's wording. Phase 6 merged; eight of eight chunks built.**
  No code change — the decision was about the plan, not the build. `TASKS.md` 6.5 now describes **incremental placement** rather than a maintained longest-path invariant, states plainly why the original wording had to go (6.8 forbids maintaining it, demonstrated with `A→B→C` minus `A→B`), and records the cost in both directions: build order can change the arrangement and a column is not always longest-path depth, but not one target ever sat left of its source in 343 random trials, so nothing ever reads wrong on screen.
  **This is the second time this build has found the *plan* at fault rather than the code** — the first being v1's spec gaps on error handling. It is worth noting which review step caught it: not reading the diff, and not the browser. It was compiling `src/lib/layout.ts` standalone and property-testing it against the rule the plan claimed. The builder's own choice to keep that module free of React and Supabase "so the algorithm can be re-derived in review" is what made that possible, and it repaid itself within the hour.
  Merged as `153fa54`. Build 487 kB on the real-client path, lint clean. **Phase 7 (Filter and the content marker) is the last chunk.**
* **2026-09-11 — Phase 7 (Filter and the content marker) built and verified. This closes the v2 sandbox build.** Branched from `claude/brave-gates-n66ll3` (Phases 1–6 merged), pushed to `claude/phase7-filter`.
  Three independent on/off toggles (Active/Upcoming/Done, all on by default) live in `uiStore.ts`, transient and not per-Area, per the spec's own instruction not to persist or scope them. `Canvas.tsx` filters which blocks render by status and passes that filtered list into the already-existing `ConnectionLines`, which hides a connection touching a hidden block for free (it looks up each endpoint in a map built from whatever block list it's given, and already skipped missing ones). Nothing in the filter path writes a block's `x`/`y` or calls anything that could — `BlockCard`'s own drag/connect logic keeps reading the full, unfiltered data through its own hooks, unchanged from Phase 6. A block holding at least one List or Note gets a small marker (top-left dot), placed there specifically so it can't collide with Upcoming's dot, Ready's badge, or Focused's outline; an empty list counts, a Waiting entry alone does not.
  No schema change, no new dependencies, four files touched (`uiStore.ts`, `Canvas.tsx`, `BlockCard.tsx`, plus one `aria-label` added to disambiguate the new filter checkboxes from `BlockEditPopup`'s same-worded status buttons). Pushed in two small commits as soon as each compiled, per this chunk's own instruction to commit early and often.
  **Verified 2026-09-11** the same way every phase since Phase 3 has been: a scratch Postgres 16 (`0001`+`0002`+`0003` applied, a stub `auth.uid()` reading `request.jwt.claims`, real `anon`/`authenticated` roles) driving the real `vite dev` app in headless Chromium via a hand-rolled PostgREST-stand-in bridge — written fresh this chunk, since no earlier phase's harness was committed to reuse. The bridge was built directly against this build's own stated lessons (throw on any unimplemented filter operator rather than silently dropping it; never treat `select`/`order`/`limit`/`offset`/`columns`/`on_conflict` as a filter; never alias an embed subquery `x` — a bridge-level smoke test specifically inserted a block with `x=111` and embedded `areas(name)` on the same query, confirming the real column survived and the embed resolved to the joined row rather than colliding). 22/22 checks passed, every one read back from the database directly, including the two the build brief called out as the ones that actually catch a false pass: a Done block connected to an Active one, toggled off, left every remaining block's `x`/`y` byte-identical (checked per-block, not "still looks the same"), and a Focused project hidden by the canvas's own Active filter was confirmed still listed on the Focus screen — proving 5.5's deliberate non-coupling actually held rather than just being uncontradicted. One overlap-shaped test bug was found and fixed in the harness, not the app, before any real check ran: newly created blocks all spawn centred on the same viewport point, so three created back-to-back landed exactly on top of each other and a double-click meant to open one opened whichever was stacked above it instead; fixed by repositioning each via direct SQL and reloading before interacting with it, which also serves as an independent confirmation that the client re-fetches real position from the server rather than trusting a stale cache. Two screenshots taken; zero browser console errors. `.env`, the harness, and `node_modules/{pg,playwright}` were never committed (`pg`/`playwright` installed `--no-save`, uninstalled after — confirmed by an empty diff on `package.json`/`package-lock.json`); `npm run build` (201.72 kB, no `.env` present, consistent with every earlier phase's own build-size note) and `npm run lint` both pass clean.
  **This is the eighth and final chunk of `TASKS.md`.** All 43 items across Phase 1, 2a, 2b, 3, 4, 5, 6, and 7 are now `[x]`. No decisions are open, and none were raised this chunk — 7.1-7.3 mapped directly onto what the spec and this build's own accumulated rules already said, with no genuinely new product question surfacing the way Phase 6's 6.5/6.8 contradiction did.

---

* **2026-09-11 — Phase 7 reviewed and passed; merged. V2 BUILD COMPLETE — all seven chunks, all 43 items.**
  Phase 7 landed in 21 minutes across three commits, pushed early, documentation finished, nothing under `supabase/migrations/`. 13 independent checks, all passing.
  **The check that mattered, and why it was the one to pick:** Phase 6 writes `x`/`y` back to `blocks` rows, so a filter that accidentally triggered a layout pass would have silently rewritten real coordinates — a data-loss bug wearing a view-concern's clothes. Toggling Done off hides the block *and* its connection (svg lines 2 → 0), and every remaining block's stored coordinates are **byte-identical**, in both directions. The implementation is immune by construction, not by care: `BlockCard` takes only its own block as a prop and fetches the *unfiltered* `useBlocks`/`useConnections` itself, so the filtered list never reaches the drag or layout path at all.
  **7.3's marker rules all hold:** a name-only block has none; an **empty** list gives it one; deleting that list removes it; a note alone gives it one; a **Waiting entry alone does not**. And it stays legible among everything else a block can now carry — the screenshot shows one block wearing Waiting's pale fill, its folded Waiting card and the new content dot at once, beside an Upcoming block whose dot is top-*right* and a Focused one with a purple outline. Four markers, four distinct positions, no collisions. **5.5 stays uncoupled:** the Focus screen still lists a Focused project whose status is filtered out on the canvas.

---

* **2026-09-11 — V2 BUILD COMPLETE AND REVIEWED. This closes the v2 sandbox build.**
  Seven chunks, 43 items, roughly $46 of builder sessions plus review. Every item ticked with an acceptance note recording what was actually observed. `npm run build` 488 kB on the real-client path; `npm run lint` clean.
  **What review actually caught — the defects:**
  * *`AutoGrowTextarea` was short by its own border width.* `style.height = scrollHeight` looks right until Tailwind's `box-sizing: border-box` is accounted for. Invisible on the borderless Note field, real on every bordered task field, and found by **measuring** — `scrollTopMax` of exactly 2 — not by reading.
  * *Enter stopped adding a task.* The `<input>`→`<textarea>` swap silently dropped the implicit form submit. Escalated rather than guessed at; Adam chose Enter-adds with Shift+Enter for a newline.
  * *A lazy-list double-create race*, recorded in Phase 2a and closed in Phase 3 by removing the mechanism rather than guarding it.
  **What review caught that mattered more — the plan itself, twice:**
  * *The Phase 2b backup decision was being made under a false premise.* "Impossible without paying" was wrong: Supabase paywalls *scheduled* backups, but `pg_dump` is free on every tier. Saying so changed the decision — Adam chose an in-database snapshot instead of accepting data loss. **The snapshot's schema choice was the security-critical part**: a snapshot of every task in `public` without RLS would have been served to the public anon key. `v1_backup` sits outside PostgREST's exposed schemas, confirmed from outside with the real key (`PGRST106`).
  * *`TASKS.md` 6.5 and 6.8 are mutually contradictory.* 6.5 wants a block's column to be its longest path; 6.8 forbids repositioning on delete. Build `A→B→C`, delete `A→B`, and no implementation can satisfy both. Demonstrated, not argued; Adam corrected the plan.
  **The process failure worth carrying further than this repo.** The v2 migration reached the live database **because a reviewer pushed to `main`** — the GitHub integration applies migrations automatically — before the backup gate it was meant to pass through. Every safeguard built for 2b assumed the migration would arrive because someone deliberately ran it. **Not holding DDL credentials is not the same as being unable to change the database.** Write access without read access is the worse asymmetry: the thing that fires it cannot see what it did.
  **And the verification failures, which are the ones this log has consistently found most instructive — this time most of them were the reviewer's own.** Three false defect reports came from a subtly wrong test harness: a silently dropped `neq.` filter made the app look like it leaked cross-Area tasks; a quoted `*` and an embed alias `x` colliding with `blocks.x` made a working Focus screen render empty. Two assertions passed **vacuously** against empty lists. One reported "completed tasks: 20" that was the row count with the `where` clause lost. And the brief twice named the **diamond** as the case distinguishing longest-path from shortest-path layering — it isn't; both paths are length 2 and the algorithms agree. The skip edge is.
  **The lesson v1 recorded, pointed inward:** a check that returns a plausible value is indistinguishable from a check that works. What caught every one of these was reading the generated SQL, or compiling the pure module and property-testing it against the rule the plan claimed — not trusting the harness's own verdict. **The single highest-leverage thing any builder did all build** was Phase 6's decision to keep `src/lib/layout.ts` free of React and Supabase "so the algorithm can be re-derived in review". It repaid itself within the hour, and it is how the 6.5/6.8 contradiction was found at all.
  **Two process changes proved themselves mid-build:** telling builders to *push early and often* (after Phase 3 ran 3.5 hours with nothing on the remote, exactly v1's Phase 0 loss waiting to happen), and telling them *finishing the chunk includes the documentation* (after Phase 4 shipped good code and skipped it). Both worked first time.
  **Outstanding, and genuinely small:** nobody has confirmed `0003` actually created `v1_backup` in the live database — the anon role cannot see that schema or `supabase_migrations` by design, so it is unverifiable from here. `select count(*) from v1_backup.tasks;` should return 20. And `claude/brave-gates-n66ll3` needs merging to `main`; this session's permission classifier blocks pushing there.
  **The next step is not code — it is using it.** That was v1's closing line and it holds again: v2 exists because real daily use of v1 produced the spec. Whatever comes next should come from using this.

* **2026-09-11 — The last outstanding item is closed: `v1_backup` is confirmed live. `select count(*) from v1_backup.tasks;` returns 20.**
  That was the one claim in the entry above that could not be checked from this session — `anon` can see neither the `v1_backup` schema nor `supabase_migrations`, which is exactly the property that makes the snapshot safe. Adam ran it against the live database; it returns **20**, matching the v1 task count recorded before `0002` rewrote task ownership from `blocks` to `lists`.
  **So `0003` applied, the snapshot is real, and it is unreachable from the public anon key.** Both halves matter: a backup nobody has read back is a belief, and a backup the anon key can read is a leak. Phase 2b's gate is now satisfied retroactively *and* verified, rather than assumed.
  Nothing in the v2 build is unconfirmed. The only step left is merging `claude/brave-gates-n66ll3` to `main`.

---

* **2026-09-12 — `main` fast-forwarded to the completed build; process assessment written for v3 planning. No application code changed.**
  `main` moved `3c86fbd → e6edee2`, a clean fast-forward of 26 commits, so the v2 build is now on the default branch and clonable for local use. **Checked before pushing, given this build's history: the migration files are byte-identical between branch and `main`** (empty diff, same three files both sides), so the Supabase integration had nothing new to apply. That check is now the standing precondition for any push to `main` here, not a one-off.
  **`v1_backup` is confirmed live** — `select count(*) from v1_backup.tasks` returns 20, matching the pre-`0002` task count. Both halves of the snapshot claim now hold: it exists, and it is unreachable from the anon key. Nothing in the v2 build is unverified.
  **A process assessment of the build was requested and written.** It was asked for as a review of `AGENTIC-BUILD-PLAYBOOK.md`. **That file does not exist and never has** — not in the tree, on disk, in any branch's history, or referenced in any `.md` here. Recorded so a future session doesn't hunt for it or assume it was lost. What actually governed this build was three things in no single place: the reviewer briefing given per session, `## Escalation criteria — Current v2` above, and rules discovered mid-build that live in this log. **That dispersal is itself the finding** — the rule that matters most, *no session may ever create a file in `supabase/migrations/`*, sits in `## Status` and in a log entry, and is **not** in the escalation criteria, which is the section a builder is pointed at.
  **The assessment's load-bearing points, kept here because v3 planning will need them:**
  * **The criteria guarded artifacts, not actions.** "Anything touching a migration" was honoured — the migration's *content* was escalated properly. Nothing named *merging* as the act that deploys, so 2a's merge applied `0002` during the chunk explicitly designated as the reversible one.
  * **The criteria constrain builders only.** The most damaging act of the build — the reviewer's push to `main` — was a reviewer action, and no criterion covers reviewer writes. The reviewer also wrote `0003`, Phase 4's documentation, and 6.5's rewording, all unconstrained.
  * **They were silent on three things that came up anyway:** a test *pass* that might be false (five of this build's verification failures were the reviewer's own harness); the plan being wrong rather than the code (it happened twice, and both were the build's highest-value moments); and documentation counting as part of "done" (Phase 4 shipped code and skipped it).
  * **Calibration, not wording, was the builder-side gap.** Phase 1's builder read "no guessing at product intent", considered the dropped Enter-to-add, and concluded it didn't qualify. The criteria need worked examples, not more categories.
  * **The 2a/2b split earned its keep and was defeated from a direction it didn't face.** It is what forced the backup to be asked as its own gated event, which is what surfaced Adam's false "backups need paying" premise and produced `v1_backup` at all. It gated the *phase*; the deploy fired on the *merge*.
  * **Phase 3 was the one visibly mis-sized chunk** — 3h37m against a 13–24 minute baseline for every other chunk. The predictor was not its item count (Phase 4 had ten items and took 24 minutes) but that it introduced two new interaction primitives rather than extending existing ones.
  * **The largest avoidable cost was rebuilding the verification harness from scratch in six separate phases**, each rebuild reintroducing bugs the last had fixed. It was never committed — the right instinct for `package.json`, the wrong one for the harness itself.
  * **This log's own most valuable convention is recording checks that lied.** Keep it verbatim in v3.
  **Nothing is open.** v2 is complete, merged, verified, and in use; the next step is v3's spec.
